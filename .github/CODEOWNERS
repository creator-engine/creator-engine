# CE-managed reviewer identities (v3 G-iii).
#
# The non-author approver of every pull request is a CE-managed reviewer
# identity: an independent, adversarially-prompted CE reviewer agent operating
# in a distinct host-bound venue (architect Q3; see
# docs/operations/GITHUB_NATIVE_COORDINATION_PROTOCOL.md and the independent
# reviewer-venue rule). Author-cannot-approve-own-PR is GitHub-intrinsic;
# enabling `require_code_owner_reviews` (done by `configure_repo()` in the merge
# batch) pins that required approval to the identities named here.
#
# These write-access collaborators are Code-Owner-eligible and distinct from
# the author identity `chmod735`.
*       @ubuntuaws745-cmyk @cedev1vps-cmd
