# Creator Engine — root signing trust anchor `ce-root-v1`
# Served at creator-engine.dev/keys/ce-root-v1 (extension-less). This is an
# OpenSSH allowed_signers file: the agent pins it to verify the signed install
# spec (docs/llms-install.md) with stock `ssh-keygen -Y verify` — no CE tooling,
# which is what breaks the install-time bootstrap circularity.
#
# Custody (Fork A, ratified 2026-06-10): Operator-authorized, orchestrator-
# generated 2026-06-10; the PRIVATE key is Operator-laptop-held at ~/.ce-keys
# (0700/0600), offline, and NEVER enters the repo or any governed seat. Signing
# the spec is a manual Operator act per release (ssh-keygen -Y sign,
# namespace `ce-spec-v1`). Single key, v1 — no rotation machinery here.
#
# Format: <principal> <keytype> <base64-key>. Verify with:
#   ssh-keygen -Y verify -f ce-root-v1 -I ce-root-v1 -n ce-spec-v1 -s <sig> < <canonical-bytes>
ce-root-v1 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG/El7UgQWNbfCv0so+P8eERg8oGkQqr6HjumrcnMLpJ
