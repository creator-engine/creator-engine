# Connect Browser

Attach only through `chrome-devtools-mcp --autoConnect` to the live
authenticated Chrome. Do not create a fresh MCP profile, do not add
`--remote-debugging-port` to the default profile, and do not use Playwright on
the real profile.
