# Capture Evidence

Capture a screenshot of the final UI state. Run a machine-readable recheck when
the surface exposes one. If the action is an identity rename, record the old
login as a historical alias instead of erasing it.
