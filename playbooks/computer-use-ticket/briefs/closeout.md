# Close Out

Return exactly one result: `done`, `blocked-human`, `blocked-substrate`, or
`no-op`. Include the screenshot reference, machine recheck reference when
available, and any historical alias note.
