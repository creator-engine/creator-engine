# Courier Forge Operation

Use this only for a contained seat that cannot directly perform the forge
operation. The contained seat drafts the operation and signals it. The
uncontained courier executes the drafted operation as the seat identity via the
held token, then records the execution evidence. Sunset this path when the
ADR-0007 egress gateway lands.
