# Base-only Refresh

Fetch the PR base, rebase the author branch, re-pin generated or hashed
artifacts that drift solely because of the rebase, run relevant validation, and
push. Do not introduce fresh feature content and do not seek fresh ratification
for a pure base-only refresh.
