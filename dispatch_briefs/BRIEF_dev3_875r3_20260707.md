# BRIEF — dev-3 — 2026-07-07 ~03:4xZ — 1 tiny unit: #875 R3 (one-line test fix, preflight blocker)

Your R2 (b746ee01) closed all 6 review findings — good work — but controller harvest preflight
found ONE new blocking failure your R2 introduced, so it was NOT pushed:

## U1 — branch `ce-228-jit-cred-injection`, worktree /var/tmp/ce-228-jit-cred-injection-r2
FAILURE: `validators/tests/unit/test_egress_host_broker.py::
test_serve_unix_socket_rejects_unexpected_peercred_by_default_when_expected_configured`
→ `OSError: AF_UNIX path too long` under pytest-xdist when the test lands on a two-digit
worker (gw10+): tmp_path + `peercred-default-reject.sock` = exactly 108 bytes = 1 over the
Linux `sun_path` usable max (107). Passes in isolation / single-digit workers — that's why you
didn't see it.
FIX (one line): in that test change the socket filename
`peercred-default-reject.sock` → `pd-rej.sock` (≤18 bytes). No logic change.
VERIFY: `validators/.venv/bin/python -m pytest validators/tests/unit/test_egress_host_broker.py -q`
green, then commit on top of b746ee01.
Signal: `READY ce-228-jit-cred-injection <new-sha>` — controller re-harvests (this unit stays
COMMIT-ONLY: gate-adjacent credential path, do NOT self-push, per the original brief).
