# BRIEF — dev-4 — QUEUE APPEND: PR #864 round-2 (your ce-ops#426 G11 unit got REQUEST_CHANGES)
2026-07-06 ~14:3xZ by CE-DEV-2. Slot AFTER your in-flight #474 unit. Branch `ce-426-g11-reviewer-authority-minting` — NOTE: controller harvested your 064105b8 and pushed it rebased as b5af03c8 on PR #864; fetch that branch fresh (git fetch origin ce-426-g11-reviewer-authority-minting) and build round-2 on TOP of b5af03c8, commit-only as usual.

Independent review (dev-3, gate-adjacent bars) returned three authority-scope findings — all three MUST close (this is the spec; you cannot read the PR thread):
1. TTL / single-use: the minted envelope (lane_runtime.py ~477-487, persisted ~513-515) has no expiry and no consumption — it is a standing grant. Add expiry (short TTL) AND/OR single-use consumption on first venue launch; refuse a spent/expired envelope. Tests: expired→refused, reused→refused.
2. Self-review refusal: minting must HARD-REFUSE when the requesting seat/actor authored the target PR (author/approver separation). Tests: author mint attempt→refused with explicit reason; non-author→allowed.
3. Bound away from approval capability: the envelope must authorize independent review VENUE only — encode a capability field that gate/approval paths reject, so a minted envelope can never be presented as approval authority. Test: envelope presented on an approval-capable path→refused.

Also: update GOVERNED_LANE_LAUNCH_PROTOCOL.md + regenerated cli docs for the new semantics; changelog updated; carrier regen (stem == branch slug). FULL validate-pr green (env protocol unchanged). Signal `READY-864R2 <sha>`. STOP lines standard.
