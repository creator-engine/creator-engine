# DISPATCH — dev-4 BATCH — 2026-07-10 — units: ce-509 design + ce-519 doctor + ce-520 reap
Role: implementer foreman (born-a-foreman: drive all three units via your workers; safe to run
concurrently — territories verified disjoint controller-side). One signal PER UNIT:
`READY-FOR-HARVEST <branch> <full-40-hex-sha>` or `BLOCKED <branch> <one-line reason>`
(pane signal only, never a committed file).

## Welcome back + housekeeping (do FIRST)
Your container was restored 2026-07-10 after the 07-09 DGX incident. Your main checkout sits on
branch ce239-wall-openbao-supplier with commit 905d55c8 SAFE plus a large pile of STAGED (A)
files including .ce/brain/assertions.yaml — that staging is residue, NOT part of ce239.
1. `git reset` the staged files (do NOT commit them; do NOT touch .ce/brain/assertions.yaml).
2. `git fetch origin main` (your origin ref is stale).
3. Leave branch ce239-wall-openbao-supplier parked as-is (commit 905d55c8 preserved); the
   controller will schedule its continuation separately. Work the three units below in
   worktrees under /var/tmp/wt-<branch> off the freshly fetched origin/main.
Your /var/tmp was reset by the restore: all pre-incident worktrees are gone; the branches that
mattered were salvaged controller-side. Nothing for you to recover.

## UNIT A — branch `ce-509-release-acceptance-design` — work class S (design doc)
Ticket (embedded): CE has a merge gate but NO ship gate. Design the release-acceptance stage:
fresh-tenant rehearsal gate + RC promotion + closure integrity. Deliverable is a DESIGN DOC
only: `docs/design/release-acceptance-stage.md`. It must cover: (1) RC→promote state machine
(what marks an RC, what evidence promotes it, where the state lives); (2) the fresh-tenant
rehearsal gate as promotion evidence (the rehearsal harness slice 1 already landed — build on
it, do not redesign it); (3) DoD-evidence-at-closure integrity (a release ticket cannot close
without linked acceptance evidence; persistent-state probes for deploy-class claims are
RATIFIED doctrine — incorporate); (4) ring-0 dogfood seat as first consumer; (5) explicit
non-goals. Design quality bar: decisions with rationale and rejected alternatives, not a
survey. Files: the design doc + changelog `.ce/changelog/ce-509-release-acceptance-design.md`
+ carrier (slug=branch). Carrier line: `- **Declared work class:** S`

## UNIT B — branch `ce-519-doctor-agent-scan-default` — work class S
Ticket (embedded, 0.3.4 tenant-rehearsal blocker): `ce doctor` skips the coding-agent CLI scan
unless require_visible_launch is set — a fresh-tenant host with no agent CLI installed sails
through doctor and then fails at first launch. Fix: make the harness/coding-agent binary check
run by DEFAULT in `validators/creator_engine_validator/doctor_runtime.py` (report-level finding
when absent, not hard-fail, unless the current design says otherwise for missing REQUIRED
runtime); add default-case tests to `validators/tests/unit/test_ce_doctor_cli.py` (fresh host,
no agent CLI → doctor reports the gap). Extend-don't-weaken. Files: those two + changelog +
carrier (slug=branch). Carrier line: `- **Declared work class:** S`

## UNIT C — branch `ce-520-reap-selfservice-kill` — work class S
Ticket (embedded, 0.3.4 tenant-rehearsal blocker): `ce reap once` ESCALATES when it meets a
live prior-version launch surface (stale tmux session from an older install) — tenant has no
self-service path to clear it. Slice 1 (THIS unit): give reap a guided/self-service kill path —
e.g. `--force`/explicit-target kill with a refusal-that-teaches message naming the exact
session and the command to clear it. Scope: `validators/creator_engine_validator/seat_reaper.py`
+ its unit tests ONLY. ⚠️ Do NOT touch launch_runtime.py (in-flight collision controller-side)
and do NOT touch install.sh or docs/llms-install.md (signed/hash-pinned — the installer
migration step is a separate release-class slice; if your smallest-good fix seems to need
either file, STOP and signal BLOCKED explaining why). Files: seat_reaper.py, its test module,
changelog, carrier (slug=branch). Carrier line: `- **Declared work class:** S`

## Preflight + signal (standing)
FULL `ce validate-pr` per unit if your seat venv can run it; environmental-only failure →
focused tests green + BLOCKED with exact failure class (controller host preflight is then the
attestation). aarch64 note: ignore ONLY the known x86-hardcoded test_install_sh_uv_* and
stale-checkout REPO_ROOT failures; NO new failures. COMMIT each unit before its signal.

## Stop lines (all units)
ce_cli.py, v3_cli.py, conveyor*.py, daemon_lease.py, validation_sandbox_*, forge/**, deploy/**,
.github/**, .ce/brain/assertions.yaml, launch_runtime.py, install.sh, docs/llms-install.md,
any file in another unit's carrier.
