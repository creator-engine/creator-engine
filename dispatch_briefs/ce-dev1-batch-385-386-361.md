# SEED BRIEF — dev-1 BATCH (4 file-disjoint items) — 2026-07-02 (day arc)

Role: implementer (you self-push your own PRs). Work the items below as
independent lanes in separate worktrees off FRESH `origin/main` (ITEM 0
continues your existing PR branch). They are file-disjoint — safe to run as
parallel subagent threads. ITEM 0 first — it unblocks a PR already in review.

## IMPORTANT — your PR #729 (ce-369 fleet-guard denylist) was CLOSED by the controller
Review found blocking issues with the snapshot representation; the branch was
deleted deliberately. Do NOT re-push `ce-369-fleet-guard-ssot-denylist`, do NOT
re-create `fleet_identity_denylist_snapshot.py` content anywhere, and do NOT
start a redo — a fresh brief with the ratified remediation direction will
follow. No action needed from you on ce-ops#369 in this batch.

## STANDING DIRECTIVES (apply to every item)
- FULL local preflight (`ce validate-pr`, CI-parity, clean tree, ONE green pass)
  before EVERY push. Do not discover gates via CI.
- Every PR: `.ce/changelog/<branch-slug>.md` + regenerated carrier via the
  `carrier_gen` API (`write_carriers(base=<merge-base-with-origin/main>)`) —
  carrier stem MUST equal the branch slug you push.
- PR body: exactly ONE `- **Declared work class:** <XS|S|M|L>` line, sized by
  the diff floor. `Refs: creator-engine/ce-ops#<n>` (cross-repo Closes is a
  no-op — use Refs).
- Commit each item and report its SHA + PR number. A done-report without a
  verifiable pushed SHA is not done.
- TERRITORY EXCLUSIONS (in-flight elsewhere — do NOT touch):
  `validators/creator_engine_validator/pr_preflight.py`, `ce_cli.py`,
  `carrier_gen.py`, `project_init.py`, `checks/fleet_manifest_guard.py`,
  anything under `docs/adr/`, and any conveyor daemon file.
  `forge_triage.py` is allowed ONLY inside ITEM 0 on its own PR branch.
  If a fix seems to require an excluded file, STOP that item and report.

## ITEM 0 — fix PR #728 review blocker (ce-ops#376 sweep, PRIORITY)
Branch: `ce-376-unscheduled-sweep` (your existing PR #728 branch — continue it).
2-review quorum returned CHANGES_REQUESTED with ONE blocker (see the review on
the PR): `_commissioned_unscheduled_items` (forge_triage.py:536-545) silently
returns `()` when the arc ticket is absent from the offline candidates payload
— `commissioned_unscheduled_count: 0` is then indistinguishable from a
verified-clean sweep. Fail-direction must flip:
- When `arc_candidate is None`, either raise `ForgeTriageError` (matching the
  invalid/ambiguous-arc precedent at ~lines 372-376) OR add an explicit
  `commissioned_unscheduled_status` / `warnings` field to
  `TriageResult.to_dict()` so zero can never masquerade as clean. Pick the one
  most consistent with how callers consume `plan_triage`; justify briefly in
  the PR.
- Update `test_commissioned_unscheduled_requires_arc_body_context` to assert
  the SIGNAL (error or status field), not just the empty tuple.
- Stay inside forge_triage.py + its test (ce_cli.py text-mode surfacing is a
  ticketed follow-up — NOT this PR).
- Full preflight, push, comment on the PR that the blocker is addressed.
Class: stays as declared on #728. A push dismisses the prior review state —
the controller re-reviews on the new head.

## ITEM 1 — ce-ops#385: authoring-doc work-class vocabulary drift (XS, docs)
Branch: `ce-385-workclass-doc-vocab`.
`docs/operations/AUTHOR_A_CE_VALID_PR.md` (~line 47-52) still documents the
RETIRED `tiny|story|feature|epic` work classes; the live gate (post-#686)
accepts XS/S/M/L. First external contributor onboards TODAY — this is a
primary authoring reference and a direct first-PR footgun.
- Update the doc to the current XS/S/M/L taxonomy.
- Grep `docs/` + `.github/` templates for other retired-vocab stragglers and
  fix prose that instructs authors (do NOT rewrite historical records: skip
  `docs/adr/`, CHANGELOG history, release notes).
- The issue's "CI guard for doc↔gate vocab reconciliation" idea is OUT of
  scope: note it in the PR body as follow-up; controller will ticket it.
Class: XS (docs — single review).

## ITEM 2 — ce-ops#386: wheel-build test race under xdist (XS, code)
Branch: `ce-386-wheelhouse-xdist-group`.
`validators/tests/unit/test_wheelhouse_built_surface.py:41` runs
`build_app_wheel_from_source(repo_root, tmp_path)` WITHOUT the
`@pytest.mark.xdist_group("wheel-build")` marker that `test_wheel_bake.py`
uses, so under xdist it races the real worktree's `validators/build` /
`*.egg-info` → intermittent false-RED in `ce validate-pr`.
- Add the marker to the wheel-build test(s) in that file so they serialize in
  the same `wheel-build` group.
- Verify: run the file + `test_wheel_bake.py` together under `-n auto` several
  times; report the runs.
Class: XS.

## ITEM 3 — ce-ops#361: codify installer-mirror backport-vs-immutability policy (S, docs/policy)
Branch: `ce-361-installer-mirror-policy`.
PR #654 retroactively mutated the published 0.2.0 installer mirror
(`docs/downloads/0.2.0/install.sh` + SHA256SUMS) to backport a security fix —
accepted case-by-case, but there is NO standing policy and it set a precedent.
- DRAFT a policy doc (suggest `docs/operations/RELEASE_MIRROR_POLICY.md`, or
  the existing policy location if one fits better — check `docs/`).
- Present the options from the issue (A: mutate+re-sign, B: immutable+
  deprecate+point-release, and any hybrid), analyze trade-offs honestly, and
  encode ONE recommended default (per user-choice architecture: options +
  recommendation as default). Cover: audit trail, consumer hash-pinning
  expectations, pre-GA vs post-GA difference, and how a backport interacts
  with the signed-release procedure (editing `docs/downloads/<ver>/` IS a
  release op — the policy must say who may perform it and how it is re-signed).
- This is a DRAFT for Operator ratification — say so in the PR body; mark the
  PR title `docs(policy):`. Do NOT modify any file under `docs/downloads/`.
Class: S (docs — single review).

## STOP LINE
Push each branch + open its PR, report SHAs + PR numbers, then stop. No
approvals, no merges, no edits outside each item's scope. Signal DONE per item
as it lands; do not hold finished items hostage to unfinished ones.
