# RESEARCH BRIEF — Controller power, continuity, and containment end-state
Dispatched: 2026-07-06 ~08:0xZ by CE-DEV-2. Role: architect_research (read-only; returns report text). Origin: Operator directive folding the 2026-07-06 codex parity-gap discussion into a governed research unit.

## Framing constraint (Operator, verbatim intent — this is the report's spine)
There is a permanent tension between usability/effectiveness/efficiency and safety. An agent must be GRANTED power to do its job; that power must be checked and guardrailed; but fully mitigating power-abuse risk means removing the power, which defeats the grant. The report must therefore evaluate every option as CAPABILITY-SHAPING (what shape of power, checked how) — never capability-removal — and must score usability/efficiency as first-class constraints alongside safety, per CE's ease-not-governance positioning and encode-the-recommendation-as-default doctrine.

## Baseline inputs (read all)
1. tmp/Codex_Controller_As_Orchestrator_Parity_Gap_06jul2026.md — the triggering report (8 proposed tickets).
2. Repo ground truth: validators/creator_engine_validator/{codex_launch_spec,claude_launch_spec,launch_runtime,codex_pretooluse}.py, .codex/requirements.toml + hooks, .claude/settings.json + hooks, docs/operations/SEAT_LAUNCH_GOVERNANCE_RUNBOOK.md, the approval-wall daemon capability-mint path, docs/adr/ADR-0005 (Ratified), docs/governance/*, GOVERNANCE.md privileged classes.
3. Doctrine (memory topic files exist for each; the repo/ADR/ce-ops record is authoritative): containment ≠ authority (contained seat = full capability; gate on role+policy); whole-fleet-to-containment ratified with controller LAST, gated on ce-ops#228 (JIT credential injection); ephemeral controllers design ce-ops#454 (gate stays singleton, signing stays persistent-controller); two-plane OS architecture ce-ops#437.

## Session findings to incorporate (already-reached positions to verify/extend, not re-derive from scratch)
A. CONTINUITY VERB (`ce takeover`): detect → select-harness → Ring-0 verify → hydrate (resume state, AWAITING-OPERATOR first) → re-arm watchers → attest (posture banner + evidence packet). Raw launch with role=controller ⇒ READ_ONLY_UNTIL_GOVERNED_LAUNCH_CONFIRMED + the refusal PRINTS the correct command (refusal-that-teaches). Continuity drill = scheduled DR test; 2026-07-06 was a failed unplanned instance (technical Operator could not discover the launch path during a quota halt).
B. SIGNING DEPUTY: ce-root-v1 key never on any seat; deputy signs SSHSIG (format constraint rules out generic vault transit — needs a small format-aware service; OpenBao custody behind it). CRITICAL EXTENSION (Operator-confirmed): a per-release Operator co-sign artifact — bound to the spec content_sha256, single-use, short-TTL, minted on the Operator's device/channel — is REQUIRED AS A CO-INPUT to the sign verb, and this gap exists TODAY for the un-contained controller (current control is purely behavioral), so the deputy applies immediately, not only at the contained end-state.
C. HOST-OPS BROKER (break-glass without raw DooD): fixed verb set of pre-written, parameter-constrained, CONVERGENT operations (each restores toward ratified known-good — an attacker spamming verbs mostly heals the system), append-only audit, rate-limit, kill-switch. Raw docker.sock mount rejected = root-equivalent (today's un-contained controller is the same exposure — acknowledged).
D. RECURSION BOTTOM-OUT: repair layers of decreasing complexity/change-rate (agents/daemons → tiny systemd-supervised broker → host OS + Operator/reprovision). Goal is not eliminating the last rung but making each rung simpler/more static than the one above (kubelet→systemd→BMC→human analogy).

## Research questions
1. Prior art survey (web, current-date grounding): how do production systems shape agent/operator authority — short-lived credential minting (Vault/OpenBao patterns, SSH CAs, GitHub App installation tokens), keyless/coordinated signing (Sigstore/witness-cosigning), k8s admission/webhook + break-glass patterns, and any published agent-fleet controller-continuity practice. Rent-or-fork-before-reinvent: which of deputy/broker/takeover can be rented or forked instead of built?
2. Verify the parity-by-enforcement-layer model (code-support / launch-wired / live-proven / promotion-approved) against our actual harness matrix; enumerate the current layer status per harness.
3. Ticket-program synthesis: consolidate the original 8 tickets + A-D into a tiered program (P0 continuity verb + drill; P1 promotion gates/evidence; P2 deputy + broker + parity matrix + remote-control policy), with per-item: acceptance bar, usability cost, safety gain, dependency edges (#228, #454, C5, OpenBao recovery state).
4. Open Operator decisions list (e.g. remote-control policy options; co-sign artifact form; broker verb list v1).

## Deliverable
A single self-contained markdown report (return it as your final text; controller persists it to .ce/state/research/CONTROLLER_POWER_CONTINUITY_RESEARCH_20260706.md). Sections: Executive summary · Tension framing · Prior art w/ citations · Current-state matrix · Design options w/ usability-vs-safety scoring · Tiered ticket program · Operator decision list. Findings only — no repo mutation, no tickets filed by you.
