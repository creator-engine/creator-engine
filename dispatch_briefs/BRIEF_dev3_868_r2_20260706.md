# BRIEF — dev-3 U3 — PR #868 round-2 fix (you authored; branch `ce-476-claim-lifecycle`) — 2026-07-06 ~14:4xZ

Queue behind your current units. Commit-only; signal `READY|BLOCKED-ENV ce-476-claim-lifecycle <sha> <evidence>`.
Base on the PR head: `git fetch origin pull/868/head`. Verdict REQUEST_CHANGES — 4 findings, all must close:

1. PACKAGING: `ce_cli.py:4776-4777` imports top-level `claim_lifecycle`, but the module lives at
   `validators/claim_lifecycle.py` — NOT under the packaged `creator_engine_validator` package
   (pyproject discovers only `creator_engine_validator*`; no py-modules entry). Installed console
   script → ModuleNotFoundError; tests mask it via PYTHONPATH=validators. Move the module into the
   package (or add packaging) AND add a test that imports via the packaged path, not PYTHONPATH.

2. STATE MACHINE: `_assert_transition_allowed` (claim_lifecycle.py:209-223) allows ANY forward
   rank jump and `abandoned` from any non-terminal state; CLI exposes all terminal states as
   direct targets with no evidence check (ce_cli.py:1949-1954, 4783-4790). Reproduced on head:
   claimed→landed, claimed→released, claimed→abandoned all allowed. Required: explicit legal-
   transition table (closed set, no rank-jump shortcut); terminal transitions (landed/released)
   must require controller/merge evidence (e.g. merge SHA verified reachable from main, or an
   evidence ref) — a claiming seat must NOT be able to forge its own release/expiry.

3. IDEMPOTENCE: same-state transition returns allowed (:212) but transition_claim still refreshes
   transitioned_at and rewrites the file (:94-103); the closeout workflow re-runs and commits on
   any file diff (ce-claim-closeout.yml:48-77). Reproduced: same landed+same SHA twice → file
   differs only by transitioned_at → rerun creates a new closeout commit. Required: same-state +
   same-SHA transition must be a byte-identical no-op (no write).

4. TESTS: cover the failure directions — illegal forward skip refused; terminal transition
   without evidence refused; closeout rerun is a no-op (assert no file change on repeat).
   Current refusal coverage is only the backward case (test_claim_lifecycle.py:61-65).
