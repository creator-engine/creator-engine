# Seed Brief: dev-1 night batch — ce-387 hold-label symmetry (fix) + ce-388 conveyor security review (report)

Two independent deliverables. You self-push your own PR for Ticket A; Ticket B produces a REPORT file (no arming, no merge). dev-2 holds the merge gate — do NOT approve/merge your own work.

Standing preflight directive (ce-ops#303): FULL `ce validate-pr` GREEN one pass before any self-push.

## Ticket A (ce-ops#387) — branch `ce-387-holdlabel-symmetry` — work class S — SELF-PUSH a PR
Gap: the PR-side awaiting-operator gate `forge/controller_inbox.py:~276-278` (`_awaiting_operator`) only checks `pr.hold_marker` (body regex) OR intersection with the 3-entry `AWAITING_OPERATOR_LABELS`. The ISSUE-side gate (`forge_triage._skip_reason` → readiness_blockers) additionally excludes the full `_BLOCKING_LABELS` family (`held`, `on hold`, `on-hold`, `status:hold`, `status/hold`, prefixes `hold:`/`hold/`/`held:`/`held/`). So a PR labeled `held`/`on-hold`/`status:hold` (no awaiting-operator label, no body marker) is NOT recognized as awaiting-operator on the PR side — asymmetry that could let a held PR slip into auto-processing. The ⏸ AWAITING-OPERATOR/hold convention is load-bearing.
Fix: align PR-side `_awaiting_operator` with issue-side coverage by reusing a SHARED hold-label set (`_BLOCKING_LABELS` ∪ `AWAITING_OPERATOR_LABELS`) across both gates so they can't drift — extract the union to one place both import. Add a regression test for a `held`/`on-hold`-labeled PR (no marker/awaiting-operator label) → classified awaiting-operator. Allowed paths: `validators/creator_engine_validator/forge/controller_inbox.py`, the shared-set source (wherever `_BLOCKING_LABELS`/`AWAITING_OPERATOR_LABELS` live — likely forge_triage.py; if extracting to a shared module, that new file is allowed), the relevant test file(s), changelog `.ce/changelog/ce-387-holdlabel-symmetry.md`, carrier `.ce/pr-manifests/ce-387-holdlabel-symmetry.md` (carrier_gen API). Evidence: targeted tests + full preflight GREEN, self-push the PR, report `PR-OPENED <n> <head-sha>`.
⚠️ Do NOT touch forge/integrator_belt.py (in-flight on another seat) or .ce/brain/assertions.yaml.

## Ticket B (ce-ops#388) — conveyor daemon INDEPENDENT SECURITY REVIEW — REPORT ONLY, no branch/push
Context: the conveyor daemon (`validators/creator_engine_validator/conveyor_daemon.py` + `forge/integrator_belt.py` pickup path + `pickup.py`/`pickup_payload_schema.py`) is a precondition-for-ARMING and needs an independent security review from a venue OTHER than its author (dev-4 authored ADR-0004 §3, PR #740). You are that independent venue. This is READ-ONLY analysis producing a written report — you do NOT modify code, arm anything, or open a PR.
Scope (ADR-0004 §3 threat model — the design survived 4 adversarial RCE rounds; verify the shipped code, on CURRENT origin/main which now includes #740's merge if landed, else review the #740 branch head): payload-as-data-only (no code/callable smuggled through the discovery payload; schema fail-closed in from_mapping); daemon-owned-working-dirs guard (fail-closed on daemon-owned paths); per-item isolation (one bad discovery item cannot abort/poison the batch — the round-3 fix); argv construction to git/gh (option-smuggling — cross-ref ce-ops#383 if you find gaps, do NOT fix here); no credential exposure in daemon subprocess env; no path traversal in prepare/land/push. For each: state CONFIRMED-SAFE / CONCERN / GAP with file:line evidence and a concrete exploit sketch for any CONCERN/GAP.
Deliverable: write the report to `.ce/state/research/CONVEYOR_SECURITY_REVIEW_dev1_20260702.md` (NOT a PR — this file is arming-bundle evidence the controller collects). End with an overall verdict: ARM-SAFE / ARM-WITH-FIXES (list them) / DO-NOT-ARM. Report the file path + verdict in your pane when done.

## Stop line (both)
No merges, no approvals of others' work, no conveyor arming, no gate/wall/daemon CONFIG changes, no toolchain self-update, no release signing. Ticket B changes NO code. If Ticket A's shared-set extraction turns out to touch a file you're unsure about, STOP and report rather than widening scope.
