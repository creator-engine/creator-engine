# BRIEF — dev-4 — 2026-07-07 ~15:xxZ — 1 unit: ce-ops#491 serialization slice 1 (hardest unit, routed to you deliberately)
Ticket substance embedded. COMMIT-ONLY (this touches the ledger-integrity path = gate-adjacent):
signal READY <branch> <sha> <evidence-path>, controller harvests. Worktree /var/tmp fresh off
origin/main. Changelog fragment + carrier slug==branch + honest work class.

## U1 — branch `ce-491-ledger-append-serialization`
PROBLEM (fresh evidence from last night, use it in the design note): `.ce/brain/assertions.yaml`
is a global hash-chained append-only ledger INSIDE the repo. Any two in-flight PRs touching it
conflict SEMANTICALLY even when git merges cleanly: (a) append-vs-append — both chain prev_hash
off the same tail (PR #874 vs main, 2026-07-07: chain forked mid-ledger, doctrine-coverage then
failed inside every example check, 37-test sweep failure, merge-queue dequeue loop); (b) mid-
chain re-pin cascade — an evidence_sha256 re-pin rewrites content_hash/prev_hash from that
record to the tail (PR #864: cascade computed on a stale ledger = CONFLICTING; required a
dedicated integrator repair round). Every ledger-touching PR currently costs a controller
serialization round.
BUILD slice 1 (choose the smallest REAL cut, design note first):
Option A (per the ticket): wire ledger appends through the mediated-append daemon AT MERGE TIME
— PRs carry append INTENTS (un-chained record payloads in a staging file, e.g.
.ce/brain/append-intents/<branch-slug>.yaml); a merge-closeout step (the #868 claim-closeout
workflow is precedent) materializes intents onto the live tail post-merge, so branches never
rewrite the chained file. Option B: make the conflict IMPOSSIBLE to miss instead — a validator
check that refuses a PR whose assertions.yaml delta re-chains from a non-current tail, with a
teaching message naming the re-chain tool. If A is bigger than one unit, SPLIT: ship B now +
design note for A, and say so in your evidence.
Failure-direction tests required (fail-closed when the tail moved). Evidence file with your
design decision + what you deferred.
