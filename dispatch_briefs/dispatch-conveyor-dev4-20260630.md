# DISPATCH — Conveyor harvest→push helper (DESIGN + slice-1) — dev-4 (foreman)

LANE: mechanize the MANUAL contained-seat harvest the controller does by hand. Tonight's harvests hit 5 repeatable footguns — the conveyor must eliminate all of them. This dispatch = (A) a DESIGN doc + sliced plan, and (B) BUILD slice-1 ONLY (the pure, testable harvest-extract+validate core). Do NOT build auto-push or any arming/daemon-loop yet (that's Operator-gated autonomy — design it, don't wire it).

## The 5 footguns the conveyor MUST handle (from real incidents)
1. Contained-seat venv is unreliable (baseline=48 false failures) → validate using a KNOWN-GOOD venv + `PYTHONPATH=<worktree>/validators`, NOT a worktree-local venv.
2. `validators/build/` is regenerated BY validate-pr → must `rm -rf validators/build validators/*.egg-info` AFTER validate, BEFORE any `git add`.
3. Carrier must be (re)generated against the FINAL clean HEAD (not while polluted) via `carrier_gen.write_carriers`, and the work-class line re-added (`- **Declared work class:** <tiny|story|...>`; use OLD names tiny/story/feature/epic for cross-gate compat).
4. Local branch name MUST == carrier stem (branch_slug). Rename branch to match before validate.
5. Branch must be rebased onto CURRENT origin/main before the path-manifest diff (stale base → phantom deletions).

## (A) Design doc (write to .ce/design/ or report inline)
The end-to-end conveyor: detect a contained seat's READY branch → `git bundle` it out of the container → land in a host worktree → rebase onto origin/main → regen carrier + work-class → rm build → validate-pr on a good venv → [STOP: controller/Operator gates push+PR+approve]. Cover: which steps are SAFE to automate (extract+validate) vs which are AUTONOMY-ARMING and stay gated (auto-push, auto-PR, auto-approve — Operator decision, like Surface-B). Sliced plan XS/S/M.

## (B) Slice-1 build (the core, testable, no arming)
A pure-ish Python helper (e.g. `validators/creator_engine_validator/conveyor.py`) that, given a worktree path + branch + base, performs steps 2-5 deterministically (rm build, ensure branch==carrier-stem, rebase-or-verify-base, regen carrier+work-class) and returns a structured "ready/not-ready + reasons" result. Unit-tested. It MUST NOT push, open PRs, or exec docker — pure local prep + a validate-pr invocation seam (inject the runner). 

## Mechanics
Worktree under /var/tmp off CURRENT origin/main, branch `ce-conveyor-harvest-core`. validate-pr via `TMPDIR=/var/tmp PYTHONPATH=$PWD/validators /workspace/creator-engine/.venv/bin/python -m creator_engine_validator.ce_cli validate-pr`. Carrier+changelog (work class story). STOP before push. Report: design summary, branch+SHA, validate-pr PASS line, the slice-1 API. Verify vs origin/main; NOT rc2; do NOT touch docs/install.sh or docs/downloads.
