# BRIEF — dev-3 — ce-467 ROUND 2 (harvest hard-stopped; two substance fixes). Safe CONCURRENT with your #476 unit (disjoint files) — fan out as foreman.
2026-07-06 ~13:4xZ by CE-DEV-2. Branch `ce-467-version-drift-gate` in your existing worktree /var/tmp/ce-467-version-drift-gate (commit 9164627e). COMMIT-ONLY → harvest. Class story.

Host re-arbitration ran your branch (rebased onto post-0.3.3-docs main) through FULL preflight: baseline 1 failure, HEAD 37 — 36 NEW, all caused by your gate. Harvest refused to push. Two findings, both yours to fix:

## Fix 1 — registration scope (the 36 cascading failures)
Your `version_drift_current_surfaces` check is `@register`ed as a general check, so it runs on EVERY `ce check` invocation over ANY path and always scans the hardcoded CURRENT_VERSION_SURFACES. Every check-examples test therefore inherits the repo's drift errors → 36 integration/unit test failures unrelated to their subjects. REQUIRED DESIGN: the drift gate must run ONCE as an explicit validate-pr / CI gate step (the ticket's ask was "wire into ce validate-pr"), NOT inside general per-path check registration. Rescope so `ce check <some-example>` never evaluates repo-wide drift. Keep a direct invocation path (e.g. dedicated subcommand or gate entrypoint) + unit tests proving: (a) drift detection still fails on a stale ref; (b) check-examples-style invocations do NOT trigger the drift scan.

## Fix 2 — make the gate pass on the CURRENT tree (12 real stale refs it found)
0.3.3 shipped after you authored (version.py now declares 0.3.3). Your gate correctly flags these 12 — bump them ALL to 0.3.3 in this same PR (your original brief anticipated this: "if current tree has drift, fix those refs"):
- deploy/oci/README.md:39,43 (your own diff — you bumped 0.2.0→0.3.2, now needs 0.3.3)
- deploy/oci/build-image.sh:12,22,24,34 (same)
- deploy/daemons/Dockerfile:3 · deploy/daemons/README.md:9 · deploy/daemons/run-daemon-container.sh:19,213
- deploy/runtime-image/Dockerfile:39 (CE_IMAGE_VERSION default) · deploy/seat-image/Dockerfile:9 (same)
These deploy/* files are now IN your PR scope (controller grants the territory; no in-flight collisions). Do NOT touch sha-pinned files.

## Also incorporate (harvester's carrier corrections, redo on your side)
Declared work class `story` (not S); changelog `kind: feat` (not fixed); changelog body must say 0.3.3.

Bar: FULL `ce validate-pr --declared-work-class story` — env-failure protocol unchanged (BLOCKED-ENV + evidence if only the known env set fails; ZERO new failures vs baseline is the bar that matters). Carrier regen (stem == branch slug). Signal `READY-467R2 <sha>`. STOP lines standard.
