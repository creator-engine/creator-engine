# BRIEF — dev-4 — BATCH: U1 ce-ops#472 (tiny) + U2 ce-ops#481 SSHSIG signing-deputy DESIGN (story, design-only)
2026-07-06 ~18:0xZ by CE-DEV-2. Contained, COMMIT-ONLY → harvest, both units. Full ticket bodies embedded below (no gh in container). U1 and U2 are file-disjoint — run concurrently as foreman.

## U1 — ce-ops#472: wheel-determinism test false-RED on stale gitignored artifacts. Branch `ce-472-wheel-test-isolation` off FRESH origin/main. Class tiny.
Fix per ticket: defensively remove/ignore validators/build/ and creator_engine_validator.egg-info/ before the determinism assertion in test_build_app_wheel_from_source_is_surface_deterministic (mirror the PR #601 isolation approach for #336). Failure-direction test: seed a stale artifact dir → test must still pass; and the determinism assertion itself still fails on a genuinely nondeterministic wheel. NOTE: this exact flake blocked dev-1's PR #859 push today (test_wheel_bake variant) — check whether test_wheel_bake.py needs the same broader footprint fix and include it if the diff stays tiny. Signal `READY-472 <sha>`.

## U2 — ce-ops#481: SSHSIG signing deputy — DESIGN DOC ONLY (like your #464 sweep design; no implementation, no key handling, DO NOT touch ~/.ce-keys or any key material). Branch `ce-481-signing-deputy-design` off FRESH origin/main. Class story. Output: docs/design/sshsig-signing-deputy.md.
Design to the ratified bar (Operator ratified the parent program as a block 2026-07-06): (a) ce-root-v1 private key unavailable to controller seats and workers — custody behind a deputy service (OpenBao-backed path preferred; interim dedicated ce-signer OS user documented as ratified bridge if OpenBao recovery blocks); (b) the sign verb requires: artifact canonical-bytes hash, spec content SHA, release id, ratification ref, AND an Operator co-sign artifact; (c) co-sign artifact = detached signed JSON {release_id, content_sha256, spec_content_sha256, expires_at, nonce, single_use_id, operator identity}, single-use, short-TTL, minted on an Operator-controlled device/channel NEVER on a controller host; (d) deputy emits OpenSSH SSHSIG with correct namespace (generic vault Transit signing is INSUFFICIENT — the deputy must be SSHSIG-format-aware); (e) value-free evidence record per signing act; (f) applies to the CURRENT un-contained controller immediately, not only the contained end-state. Ground the design in the actual install-chain verification code (find where install.sh/spec verification checks the SSHSIG against the ce-root-v1 pubkey) and the release-signing procedure the repo documents. Include: threat model (what each control defeats), the verb API, sequence diagrams for a release ceremony, migration path from today's key-on-disk, and open questions for the Operator. Signal `READY-481 <sha>`.

Bars both: FULL ce validate-pr GREEN (env protocol: BLOCKED-ENV + evidence if only the known env set fails); carrier stem == branch slug; changelog each. STOP lines standard; U2 touches ONLY the design doc + gate artifacts.
