TICKET ce-ops#147 — SSOT expansion: extend the identity-registry schema with new dimensions. WORK CLASS: story. SEAT: dev-4 (contained, COMMIT-ONLY — implement + commit + preflight, then STOP; do NOT push).
WORKTREE: `git fetch origin main && git worktree add /tmp/wt-ce147 -b ce147-identity-registry-dimensions origin/main` (use /tmp if /workspace perms block in-tree worktrees).
SCOPE — items 1-5 ONLY (DEFER item 6, the declared-vs-actual GH-API drift check, to a follow-on — a commit-only contained seat has no GH egress):
- `schemas/identity-registry.schema.yaml`: add `repos` top-level array ($def repo_inventory_entry: name, visibility, owner, purpose, access); add optional `email_of_record` to the account $def; extend the token $def with `permission_scopes`, `resource_owner`, `expiry`; extend host_topology_entry with optional `credential_pointers` (list of openbao-ref strings); add an `openbao_pointer` $def (mount, path, policy, rotation_owner).
- `docs/governance/identity-registry.example.yaml`: add example entries for every new field/array so the inline CI step "YAML schema check — identity registry" (validates example-vs-schema) stays GREEN.
- `validators/tests/unit/test_identity_registry_schema.py`: plain pytest asserting the extended schema validates well-formed examples and rejects malformed ones. NO @register decorator (this is NOT a registered validator check); do NOT touch checks/__init__.py.
CONFIDENTIALITY (creator-engine is PUBLIC): the example.yaml MUST use only EXAMPLE_-prefixed / placeholder values — NO real fleet identities, IPs, host names, account logins, App/installation IDs, key paths, or OpenBao paths. The authoritative registry with real values lives INTERNALLY (ce-ops#269). The public-docs confidentiality guard will reject ce-ops# refs / real identifiers.
HARD EXCLUSIONS: .github/**, deploy/systemd/**, tools/egress-broker/**, AGENTS.md, .claude/agents, .ce/brain/assertions.yaml, docs/llms-install.md, checks/__init__.py.
GOVERNANCE: carriers `.ce/changelog/ce147-*.md` + `.ce/pr-manifests/ce147-*.md` (issue: ce-ops#147). Put `- **Declared work class:** story` in a PR_BODY.md note for the controller's intake-push, BUT do NOT `git add` PR_BODY.md into the commit (it causes merge conflicts — keep it untracked).
PREFLIGHT (FULL): `PYTHONPATH=validators python3 -m creator_engine_validator.ce_cli validate-pr --base origin/main --declared-work-class story`. COMMIT-ONLY: commit (do NOT add PR_BODY.md) + preflight green, then STOP and report DONE. Do NOT push.
