# BRIEF — dev-3 — ce-ops#467 feature slice 1: version-drift CI gate (unsigned-docs sibling of #457)
2026-07-06 ~09:0xZ. Role: implementer, contained, COMMIT-ONLY → harvest. Branch `ce-467-version-drift-gate` off FRESH origin/main. Your #461 unit is PARKED pending the template fix (correct stop, good find) — this replaces it.

Context: ce-ops#467 (docs currency): README sat at 0.3.0 for two releases because README/site version refs are in nobody's release-touch set and no gate catches drift. The immediate bump landed (#847). This slice = the durable gate.

Scope: a validate-pr/CI check (pattern: sibling of #457's prose-lint, UNSIGNED surfaces only) that scans a declared surface set — README.md, docs/ site pages, deploy script default image tags (e.g. deploy/daemons/run-daemon-container.sh CE_DAEMON_IMAGE default — see ce-ops#467 comments) — for version strings that must track the current release version (source of truth: the version the repo declares, find where release-finalize reads it), failing with file:line when a current-version claim is stale. MUST distinguish current-version claims from historical mentions (changelog entries, "as of 0.3.0" history stay legal) — use an explicit allowlist/annotation mechanism, shrink-only. Wire into ce validate-pr + the CI workflow ONLY if workflows need no permission changes (read-only check). Tests: stale-ref fails, historical-mention passes, current passes.

Bar: FULL ce validate-pr GREEN one pass (your new gate must pass on the CURRENT tree — if current tree has drift, fix those refs in this unit); carrier (stem == branch slug); changelog; class story. COMMIT-ONLY; signal `READY <sha>`. STOP lines standard + no sha-pinned files (0.3.3 release in flight — do not touch docs/downloads/*, .ce/release-staging/*, llms-install.md).
