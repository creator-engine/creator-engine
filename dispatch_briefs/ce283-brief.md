TICKET ce-ops#283 — Public-docs guard: block net-new content in INTERNAL-ONLY trees (docs/operations, docs/delivery) per the ce-ops#249 whole-tree ruling. WORK CLASS: story. SEAT: dev-4 (commit-only — implement + commit locally + preflight, then STOP; do NOT push).
WORKTREE: `git worktree add ../wt-ce283 -b ce283-docs-internal-tree-guard` (work there).
CONTEXT: ce-ops#249 ruled docs/operations/ and docs/delivery/ are INTERNAL-ONLY (belong in ce-ops, not the public creator-engine repo), but nothing enforces it — a seat re-introduced internal docs into the public repo (ce-ops#91/PR #544). The existing guard (validators/tests/unit/test_public_docs_confidentiality.py) only catches ce-ops# refs + host identifiers, not net-new files in internal-only trees.
SCOPE:
- Extend `validators/tests/unit/test_public_docs_confidentiality.py` with a NEW test (e.g. `test_no_net_new_files_in_internal_only_trees`) that HARD-FAILS if any file exists under `docs/operations/**` or `docs/delivery/**` that is NOT in a `_KNOWN_OPERATIONS_EXCEPTIONS` / `_KNOWN_DELIVERY_EXCEPTIONS` allowlist (RATCHET pattern matching the existing `_KNOWN_PENDING` — seed the allowlist with the CURRENT files so the test passes now, and any NEW file fails until explicitly allowlisted or relocated).
- You MAY factor the detection into a standalone module `validators/creator_engine_validator/public_docs_internal_tree_guard.py` if cleaner.
- (Stretch, optional) add internal-ops vocabulary heuristic flags.
HARD EXCLUSIONS: this is a PYTEST extension, NOT a registered validator check — do NOT add an `@register` decorator and do NOT touch `validators/creator_engine_validator/checks/__init__.py` (that would collide with in-flight #546/#273 and the count-assertion tests). Also: AGENTS.md, .github/**, .ce/brain/assertions.yaml, tools/egress-broker/**.
GOVERNANCE: carriers `.ce/changelog/ce283-*.md` + `.ce/pr-manifests/ce283-*.md` (front matter incl issue: ce-ops#283); the PR body line `- **Declared work class:** story` (include it in a PR_BODY.md note for the controller to use at intake-push).
PREFLIGHT (FULL): `PYTHONPATH=validators python3 -m creator_engine_validator.ce_cli validate-pr --base origin/main --declared-work-class story`. COMMIT-ONLY: commit locally + preflight green, then STOP and report DONE. Do NOT push.
