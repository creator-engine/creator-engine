# BRIEF — dev-1 — Surface B arming wiring: broker run-mode deployment reconciliation (ce-ops#356)

Non-contained, SELF-PUSH as ce-dev-1. Fresh branch `ce-armB-broker-runmode` off CURRENT origin/main (git fetch origin main first). Drive to a green PR.

## Mission
Make the DEPLOYED per-seat self-review brokers honor run-mode, so the Operator's arming gesture (`CE_EGRESS_RUN_MODE=strangeLoop` + restart) actually enables autonomous APPROVE. Today the live per-seat units (e.g. `ce-egress-self-review-dev3.service` on this VPS) launch the broker WITHOUT `--run-mode` → the daemon starts `run_mode=None` → `_is_strangeloop(None)` is False → APPROVE is ALWAYS refused, even if someone sets the env. So APPROVE is currently un-armable. You make it ARMABLE; you arm nothing.

## SAFETY INVARIANT (non-negotiable — arming control)
- Default MUST be `dev` → APPROVE refused. Only an explicit `CE_EGRESS_RUN_MODE=strangeLoop` arms.
- You MUST NOT set strangeLoop anywhere committed, and you MUST NOT redeploy/restart the live brokers — the actual env-flip + restart is the Operator's reserved arming act. You only fix the WIRING so that act will work.
- The author≠approver host-side wall + envelope validation are untouched and must remain.

## Investigate first (read-only)
1. Inspect the actual deployed unit(s): `systemctl --user cat ce-egress-self-review-dev3.service` (and any sibling per-seat units) on this VPS. Note the exact ExecStart — confirm it omits `--run-mode` and how it sources config (`--config /etc/ce-egress/broker-dev3.json`).
2. Find how per-seat units are GENERATED: is there a template (`deploy/systemd/ce-egress-self-review.service` has `--run-mode ${CE_EGRESS_RUN_MODE}` + `EnvironmentFile=...`), an installer script, or are they hand-written? Locate the generator/source of truth in the repo (`deploy/systemd/`, install scripts).

## The fix
Reconcile the per-seat broker launch with the run-mode-aware pattern the generic unit already documents:
- The broker launch must pass `--run-mode ${CE_EGRESS_RUN_MODE}` (or read run_mode from its config), sourced from an `EnvironmentFile` (e.g. `%h/.config/creator-engine/ce-egress-self-review.env`) that defaults `CE_EGRESS_RUN_MODE=dev`.
- Update the unit TEMPLATE and/or the per-seat GENERATOR so every seat's self-review broker inherits this wiring. If the per-seat units are generated by a script/installer, fix the generator so regenerated units carry the run-mode wiring; if hand-written templates, fix the template + document regeneration.
- Keep `dev` the default at every layer (EnvironmentFile default, code default).

## Deliverable
A green PR that fixes the wiring (template/generator/installer in `deploy/systemd/` + any install script) so that the arming procedure becomes: Operator sets `CE_EGRESS_RUN_MODE=strangeLoop` in the EnvironmentFile + restarts the broker → APPROVE armed. Include:
- A short `deploy/systemd/` doc/README note stating the exact arming + rollback procedure (env=strangeLoop + restart to arm; env=dev + restart to disarm).
- Tests or a lint proving the generated/template unit passes run-mode from the env and defaults dev. Do NOT actually restart live brokers.

## PR
- Title: `fix(egress): wire run-mode into deployed self-review broker units (ce-ops#356 / Surface-B arming)`.
- Body: one-line summary; `- **Declared work class:** story` (state exactly); Governance: tracks ce-ops#356 / #313 arming-completion, notes it stays INERT (default dev → APPROVE refused), the author≠approver wall is untouched, and the Operator arming act (env-flip + restart) is NOT performed here.
- Path-manifest + changelog carriers matching base..HEAD. Run FULL `ce validate-pr` GREEN locally before pushing. Push as ce-dev-1, then STOP (controller reviews + gates). Report the PR number + SHA.
