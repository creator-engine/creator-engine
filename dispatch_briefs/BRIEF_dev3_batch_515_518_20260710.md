# DISPATCH — dev-3 BATCH — 2026-07-10 — units: ce-515 flaky fix + ce-518 reconcile sweep s1
Role: implementer, born-a-foreman: drive BOTH units, one signal PER UNIT before session end.
Signal format (PANE SIGNAL ONLY, never a committed file):
`READY-FOR-HARVEST <branch> <full-40-hex-sha>` or `BLOCKED <branch> <one-line reason>`

## Preconditions (both units)
- `git fetch origin main` first; branch each unit off the fetched origin/main (independent
  branches, NOT stacked). Worktrees under /var/tmp/wt-<branch> (writable), never /workspace.
- Do NOT touch `.ce/brain/assertions.yaml` in either unit.
- Seat venv is broken (missing .venv/bin/python): full validate-pr will fail environmentally.
  Run the FOCUSED tests for your changes green, then signal BLOCKED with reason
  `seat-venv-python-missing` per unit — the controller runs the authoritative host preflight.
  NEVER weaken or skip existing tests. COMMIT each unit before its signal.

## UNIT 1 — branch `ce-515-copytree-race-fix` — work class XS
Problem (embedded): `test_release_finalize_docs_copy_passes_release_guards` (find it by name;
it lives in the release-finalize test module) is flaky under xdist: it calls
`shutil.copytree(REPO_ROOT, ...)` with no ignore filter, and concurrent workers mutate
`validators/build/`, `dist/`, `__pycache__/`, `*.egg-info` during enumeration →
FileNotFoundError/broken-symlink races. Two wasted preflight round-trips on 07-08/07-09.
Fix (smallest-good): pass `ignore=shutil.ignore_patterns('build', 'dist', '__pycache__',
'*.egg-info')` to that copytree (or derive the copy list from `git ls-files` if the test
already shells to git — do NOT introduce a new git dependency just for this). Scope is the
TEST module only; no production code changes.
Allowed writes: the one test module containing that test, `.ce/changelog/ce-515-copytree-race-fix.md`,
carrier `.ce/pr-manifests/ce-515-copytree-race-fix.md` (slug=branch). Carrier must contain exactly:
`- **Declared work class:** XS`

## UNIT 2 — branch `ce-518-stale-ticket-reconcile-s1` — work class S
Problem (embedded): tickets whose work already landed stay open when the landing PR omits the
ticket reference (cross-repo Closes is a no-op here). Three live instances caught on 07-10
alone. Slice 1 = a REPORT-ONLY reconciliation tool; no auto-close, no cron wiring, no daemon.
Build: `validators/creator_engine_validator/ticket_reconcile.py` (NEW) —
- Input: a list of open tickets (number, title, optional branch-slug hints) and a list of
  merged PRs (number, title, head branch, body) — both as plain data (JSON/dicts), so the
  module is API-agnostic and fully testable offline. A thin `__main__` that reads two JSON
  files and prints the report is enough; no network calls in this slice.
- Matching heuristics, conservative, each match tagged with its evidence: (a) PR head branch
  contains `ce-<ticket#>-`; (b) PR title/body contains `ce-ops#<ticket#>` or `#<ticket#>`
  with the ticket-repo qualifier; (c) exact ticket-title token overlap above a threshold is
  NOT used (too fuzzy — leave it out).
- Output: deterministic report lines `STALE-OPEN ce-ops#<n> <- PR#<m> (<evidence-tag>)`,
  sorted, plus a machine-readable JSON mode.
Tests: `validators/tests/unit/test_ticket_reconcile.py` (NEW) — fixture-driven: branch-slug
match, body-ref match, no-match stays silent, dedup when both heuristics fire, JSON mode.
Allowed writes: those two NEW files, `.ce/changelog/ce-518-stale-ticket-reconcile-s1.md`,
carrier `.ce/pr-manifests/ce-518-stale-ticket-reconcile-s1.md` (slug=branch) containing exactly:
`- **Declared work class:** S`

## Stop lines (both units)
ce_cli.py, v3_cli.py, conveyor*.py, daemon_lease.py, validation_sandbox_*, forge/**, deploy/**,
docs/**, .github/**, schemas/**, docs/llms-install.md (signed), any file listed in another
unit's carrier. Disjointness verified controller-side against all in-flight territories.
