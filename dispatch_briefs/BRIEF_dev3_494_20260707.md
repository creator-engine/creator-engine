# BRIEF — dev-3 — 2026-07-07 ~15:xxZ — 1 unit: ce-ops#494 remediation slice (extends your #489 territory)
Ticket embedded (no forge read needed). Commit-only? NO — this is code-class non-gate-adjacent:
broker self-push per your D7 authority (push + open PR, signal READY <branch> <sha> PR=<url>).
Worktree /var/tmp fresh off origin/main. Changelog fragment + carrier slug==branch + honest class.

## U1 — branch `ce-494-onboard-workflow-refresh`
CONTEXT: `ce onboard`'s rendered adoption workflow had a broken spec-canonicalization for ~18
days (a `\1` regex backref in the template collapsed to a control byte because the template
f-string is non-raw; fixed on main by #859). Every repo onboarded in the window carries the
broken workflow: its CI spec-verify step computes canonical bytes that differ from the signer's
(release_publish.py uses raw r"\1<published-with-this-spec>") → content_sha256 mismatch →
adoption CI fails closed. The first live tenant repo IS affected.
BUILD (smallest real slice):
1. `ce onboard --refresh-workflow` (or extend the existing apply legs with a refresh lane —
   follow the codebase's grain in onboard_apply.py, you know it from #489): re-renders ONLY the
   adoption workflow file from the current template into an already-onboarded repo, idempotent
   (no-op when the rendered content already matches), refuses with a teaching message when the
   repo was never onboarded.
2. PARITY GUARD (the durable regression stopper): a unit test asserting the template's rendered
   canonicalization code produces byte-identical canonical output to release_publish.py's
   canonicalization on a fixture spec (this is the test that would have caught the bug).
3. Tests: refresh-applied, refresh-noop-when-current, refresh-refused-not-onboarded.
Refusal/UX text bars: teaching tone, no "bet"/"appetite", no ce-ops# refs user-visible.
