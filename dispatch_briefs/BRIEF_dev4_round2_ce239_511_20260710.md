# DISPATCH — dev-4 ROUND 2 — 2026-07-10 — units: ce239 completion + ce-511 seat-watch s2
Role: implementer foreman. One signal per unit:
`READY-FOR-HARVEST <branch> <full-40-hex-sha>` / `BLOCKED <branch> <one-line-reason>`

## UNIT A — branch `ce239-wall-openbao-supplier` (EXISTING, parked at 905d55c8) — class S
Your own pre-incident unit: approval-wall OpenBao secret-supplier gate (original brief
delivered alongside at /var/tmp/BRIEF_ce388_d1_pickup_openbao_supplier.md — re-read it; its
terms still bind). Continue from commit 905d55c8 in a FRESH worktree off that branch
(/var/tmp/wt-ce239-wall-openbao-supplier — your old /var/tmp worktrees died with the container
restore). First `git fetch origin main` and rebase the branch onto current origin/main —
main has moved substantially (incl. #926 remint + #927 chown fixes in the same wall territory:
reconcile, extend-don't-weaken). Then finish the unit per the original brief and signal.

## UNIT B — branch `ce-511-seatwatch-s2-events` — class S
Ticket (embedded, ce-ops#511 slice 2; slice 1 = #915 observe-only, merged): seat-watch daemon
follow-through. THIS slice: (1) structured event emission — idle-without-signal and
dispatch-undelivered detections write durable event records (JSONL under the state root the
slice-1 code already uses; include seat id, class, evidence, timestamp); (2) auto-restart
posture — the watcher process itself is supervisable (clean exit codes + a systemd unit FILE
example under deploy/seat-watch/ mirroring how slice 1 ships its assets); (3) tests for both.
NOT in slice: FleetView/cockpit integration, forge-side consumers, live IaC rollout.
Files: the slice-1 seat-watch module + its tests, deploy/seat-watch/** (additive),
changelog + carrier (slug=branch, `- **Declared work class:** S`).

## Standing terms
Both units: worktrees under /var/tmp, off freshly fetched origin/main (Unit A: rebase existing
branch). aarch64 known-failure carve-out applies; focused tests green + BLOCKED(env) if full
validate-pr is env-blocked; COMMIT before signalling. Stop lines: ce_cli.py, v3_cli.py,
conveyor*.py, validation_sandbox_*, forge/** (code), .github/**, .ce/brain/assertions.yaml,
launch_runtime.py, install.sh, docs/llms-install.md, seat_reaper.py, doctor_runtime.py,
docs/design/** (round-1 territory). daemon_lease.py: Unit A may READ it; modify ONLY if the
original ce388-d1 brief explicitly authorizes it.
