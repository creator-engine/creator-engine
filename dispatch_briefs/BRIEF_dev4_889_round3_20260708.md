# BRIEF — dev-4 — 2026-07-08 — 1 SMALL unit: PR #889 round-3 (one narrow residual)

Your revision resolved everything except one residual of B1. Surgical fix on the SAME
branch `ce-491-optiona-merge-intent`. COMMIT-ONLY: signal `READY <branch> <sha> <path>`.
Fresh /var/tmp worktree; fetch origin, base on the PR head 913f48ea9. Commit early;
PYTEST_ADDOPTS="-n 2". DESIGN-ONLY.

## The residual (two edits in docs/design/ce-491-optiona-merge-intent.md)

1. PREREQUISITE CORRECTION (~L105-107): the doc claims the tracked
   brain_append_intent.schema.yaml pins four intent kinds. On main TODAY it pins TWO
   (active_assertion_append, ce411_supersede_pair); decision_append and lesson_append
   are introduced by PR #888 (branch ce-488-memory-layer-slice1, head 50acee07 —
   approved, merging tonight; by your rebase time it is likely on main — CHECK, and
   word accordingly). Add an explicit prerequisite block: this design's decision/lesson
   intent kinds depend on #888's schema expansion; name the PR and the schema file.
2. DECISION/LESSON PAYLOAD MAPPING (near the reconciliation table, ~L120-122): add a
   payload sub-table for decision_append and lesson_append intents mapping intent
   payload fields → materialized record fields — explicitly including where
   `authority` (decision) and `source` (lesson) come from in the intent YAML. Mirror
   the level of detail the active_assertion_append worked example already has (your
   own #888 worktree /var/tmp/ce-488-memory-layer-slice1 has the authoritative
   decision_content/lesson_content payload shapes — use those exact field names).

Everything else in the doc is CONFIRMED RESOLVED — do not restructure, do not touch
other sections. Extend the changelog fragment with one revision line. Carrier
unchanged unless paths change (they must not).

Standing preflight directive (ce-ops#303): FULL local preflight before
commit-for-harvest (-n 2 cap; ENV-SKIP fallback with everything else green).

STOP LINE: the two edits above + changelog line ONLY. No code/schema files, no pushes,
no gate acts.
