# BRIEF — dev-4 — 2026-07-07 ~17:xxZ — 1 unit: ce-ops#488 memory-layer slice 1 (hard unit)
Fresh session; ticket substance embedded (you cannot read ce-ops). COMMIT-ONLY (ledger/takeover-
adjacent): signal READY <branch> <sha> <evidence-path>; controller harvests. Worktree /var/tmp
fresh off origin/main. Changelog fragment + carrier slug==branch + honest work class.
NOTE: main now (or shortly) carries a validate-pr gate that refuses assertions.yaml deltas
chained from a stale tail (PR #882) — if your unit appends brain records, base on the FRESHEST
main and expect to re-chain at harvest if main moved.

## U1 — branch `ce-488-memory-layer-slice1`
GOAL (productize controller-agnostic memory — the hydration contract `ce takeover` needs):
1. Two NEW brain record kinds through the EXISTING mediated-append path (ADR-0005; the same
   machinery as brain-assertion records in .ce/brain/): `decision` records (operator decisions
   with date, scope, statement, supersedes-ref) and `lesson` records (feedback/corrections with
   why + how-to-apply). Schema-validated like existing kinds; follow the codebase's grain in
   validators/creator_engine_validator/brain_runtime.py + checks/ce_brain_assertions.py.
2. A MACHINE-READABLE HYDRATION CONTRACT: `ce takeover`'s Hydrate phase (takeover_runtime.py)
   consumes a deterministic, ordered digest of (active decisions + active lessons + newest
   resume-state pointer) — define the contract as a function/artifact (e.g. `ce brain hydrate
   --json` emitting the ordered set) and wire takeover's Hydrate to call it. The contract is
   the deliverable; a takeover controller must be able to reconstruct operating context from
   artifacts alone (this gap was observed live 2026-07-07: a standby controller could not run
   forge housekeeping because the workflow lived only in session memory).
3. Failure directions: invalid record kinds refused via the existing validation path; hydrate
   with an empty/absent ledger returns a well-formed empty contract (not a crash); takeover
   Hydrate surfaces WHAT it hydrated in its evidence output.
4. Tests for: both record kinds round-trip through mediated append; hydrate output deterministic
   ordering; empty-ledger hydrate; takeover Hydrate consumes the contract.
If the full slice exceeds one unit: ship record kinds + `ce brain hydrate` and DEFER the
takeover wiring with a design note — say so in your evidence.
