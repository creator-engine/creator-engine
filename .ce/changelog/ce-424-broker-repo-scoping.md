# ce-424-broker-repo-scoping

- Added per-seat egress broker repo configuration with top-level repo fallback.
- Fail closed when a seat has no repo and no top-level default exists.
- Covered resolved-repo push, PR, self-review request paths, and example config parsing.
