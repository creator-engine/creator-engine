Add a host-broker JIT credential lane for `model-api` and `forge-scoped` classes, including
per-seat allowlists, request-time minting over the broker socket, TTL/explicit revoke state,
single-active replacement, and failure-direction coverage for no env/Docker delivery,
unknown-class refusal, TTL expiry, and flock serialization.
