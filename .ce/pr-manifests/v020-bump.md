# PR path manifest — `v020-bump` · 0.1.0 → 0.2.0 semver bump

Per-PR carrier (`.ce/pr-manifests/<branch-slug>.md`, the ce-ops#21 convention). CI runs:

```bash
verify-path-manifest --base <PR base sha> --manifest-dir .ce/pr-manifests --head-ref v020-bump
```

and requires this PR's `base..HEAD` diff (computed `--no-renames`, so the wheel
rename is a delete + add) to equal exactly the authorized path-set below (the carrier
lists itself); the repo-wide fidelity scan requires the declared count and SHA256 to
match the fenced block.

Ratified gate:
Operator-RATIFIED semver milestone policy + 0.2.0 bump gate on **ce-ops#48**
(issue body sha256 `a4ebf7928cb159cb340eac80e3ac35688e643133cb3970be325cce64a81808e9`,
Part 3). Scope record `v020-bump` (`.ce/state/scopes/v020-bump.scope.yaml`,
ratified_scope_sha `7071a6b7c186a02d117d2266b5ec5fb9a90ba17175f18e13e995388ae1d2f248`).

Milestone citation (the bump's evidence, per Part 3):
**0.1.0 → 0.2.0 = the dev-process self-hosting milestone** — the #14 retirement
condition, satisfied 2026-06-12. The dev process is now fully v3-native (12/12 of
today's gates), the last carve-out (codex seats) closed by the #215 G1 Codex drive
bridge — and this run is itself the first live v3-native codex drive through that
bridge's surface. The first artifact to carry `0.2.0+<sha>` is the wheel of the gate
that ends the v1 era's last dependency.

Base:
`f57c15916bca4e1ba220f2329eef3775ffdfcf79` (current `main` at handoff, the merge-parent
HEAD baked into `_version.py` as `BUILD_GIT_SHA`).

Scope adjudication:
IN: `version.py` `__version__` 0.1.0 → 0.2.0 (the single source of truth; the derived
`+<short-sha>` machinery itself takes ZERO code changes); `pyproject.toml [project].version`
co-move; the generated `_version.py` regenerated by the sanctioned generator
(`python -m creator_engine_validator.version --write-build-file --allow-dirty`) so its
`SEMVER` tracks pyproject (`verify_generated_version` parity) and `BUILD_GIT_SHA` re-bakes
to this gate's merge-parent HEAD — this regeneration is the "committed-before-wheel-build"
step inherent to the wheel rebuild, not a feature change; the two version-literal tests
(`test_packaging_contract.py`, `test_version_surface.py`) bumped 0.1.0 → 0.2.0 in their
fixtures; the wheelhouse app-wheel rebuilt from this branch's source (filename moves to
0.2.0: old wheel deleted, new added, `SHA256SUMS` re-pinned; `validators/build/` cleaned
before commit).

OUT: any feature change; any other version literal invented; `uv.lock` self-version (not
enforced by any contract test — left untouched per "nothing else changes"); the `+<sha>`
derivation logic in `version.py`.

Per-file purpose:
- **`.ce/pr-manifests/v020-bump.md`** *(A)* — this carrier (self-inclusive).
- **`validators/creator_engine_validator/version.py`** *(M)* — `__version__` 0.1.0 → 0.2.0
  (the single source of truth; `SEMVER` aliases it; derivation logic unchanged).
- **`validators/pyproject.toml`** *(M)* — `[project].version` 0.1.0 → 0.2.0.
- **`validators/creator_engine_validator/_version.py`** *(M)* — regenerated baked build
  identity: `SEMVER = "0.2.0"` (parity with pyproject), `BUILD_GIT_SHA` re-baked to the
  gate merge-parent `f57c159`. Written by the generator, not by hand.
- **`validators/tests/unit/test_version_surface.py`** *(M)* — version-literal fixtures
  bumped 0.1.0 → 0.2.0.
- **`validators/tests/unit/test_packaging_contract.py`** *(M)* — version-literal fixtures
  bumped 0.1.0 → 0.2.0.
- **`validators/wheelhouse/creator_engine_validator-0.1.0-py3-none-any.whl`** *(D)* — the
  superseded 0.1.0 app wheel, removed.
- **`validators/wheelhouse/creator_engine_validator-0.2.0-py3-none-any.whl`** *(A)* — the
  0.2.0 app wheel rebuilt from this branch's source (byte-parity with source verified).
- **`validators/wheelhouse/SHA256SUMS`** *(M)* — re-pinned for the rebuilt 0.2.0 wheel.

Canonicalization:
`sha256("\n".join(sorted(unique_paths)) + "\n")`.

AUTHORIZED_PATHS_COUNT=9

AUTHORIZED_PATHS_SHA256=2f3982cbf2ef8c39e93802e8b522e7975ca76b765546894d02e0cca6cb6da12f

```text
.ce/pr-manifests/v020-bump.md
validators/creator_engine_validator/_version.py
validators/creator_engine_validator/version.py
validators/pyproject.toml
validators/tests/unit/test_packaging_contract.py
validators/tests/unit/test_version_surface.py
validators/wheelhouse/SHA256SUMS
validators/wheelhouse/creator_engine_validator-0.1.0-py3-none-any.whl
validators/wheelhouse/creator_engine_validator-0.2.0-py3-none-any.whl
```
