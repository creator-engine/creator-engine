# RESUME STATE — CE-DEV-2 Orchestrator — NIGHT-ARC — 2026-06-30 ~19:00Z

> NEWEST. Supersedes 1838Z. Operator signed out (~18:02Z, "drive to completion"). Full autonomy (G1–G7 + R1/Option A + R5). Read this + MEMORY.md + briefs: `N0-automation-inventory-CORRECTED-20260630.md`, `L7-auto-release-design-20260630.md`.
> ⚠️ Main checkout = stale `ce-release-0.3.1-rc2`. ALWAYS verify vs `origin/main`. This trap caused N0-audit + #695-review false-negatives.

## ✅ SHIPPED TO main (origin/main = ff70eea69)
- **Onboarding GO-LIVE complete**: #695 N1a re-sign → N1b live redeploy (live content_sha256=865ba4f4 + openssh prereq) → N1c verify (resolve_latest_signed_release → VerifiedRelease 0.3.1). External install now accepts the fixed signed spec.
- **#694 L2 auto-merge GO-LIVE** — docs-class XS/S auto-merge armed (RUN_MODE=ceo + ENABLING_REF=ce-ops#356). SAFETY-reviewed 6/6. Rollback=unset RUN_MODE / kill-switch=true.

## 🔧 DAEMON RECOVERY (this cycle)
Wall queue-daemon (old PID 43010) **died 18:36 on `gh: Bad credentials 401`** (2-day-old token expired; failed CLOSED = safe). **Restarted** via `bash /home/cedev2/ce-wall-daemon-launch.sh` (re-sources current overwatch.env) → **new PID 3292408, clean passes**. ⚠️ CHECK PID each fleet-check; restart = that launcher backgrounded. [[ce-wall-daemon-token-expiry-restart]]

## 🔄 UPDATES (≤22:15Z)
- **N1d (re-scoped) PUSHED → PR #702** (reviewer a7574d81 running). Clean Python-only, validate-pr GREEN, 180 unit tests. → on APPROVE, approve as ce-dev-2.
- **Harvest grind = 6 validate-pr rounds.** Footguns hit (ALL in the manual harvest flow): (1) unreliable container venv (baseline=48 false failures) → validate on DGX instead; (2) rc2-install work-class gate rejects XS/S → use story/tiny; (3) `validators/build/` regenerated BY validate-pr → `git add -A` swept 344 artifacts in → must `rm -rf validators/build` AFTER validate, before add; (4) carrier regen ran while HEAD polluted → 344-path carrier; (5) **local branch name must == carrier stem** (had ce-n1d-rescoped vs carrier ce-n1d-sshkeygen-preflight) — [[ce-harvest-carrier-slug-must-match-branch]]. **→ STRONG case to build the conveyor harvest→push daemon (mechanize all 5).**
- **dev-4** working L7-injection-cleanup (branch ce-l7-injection-cleanup). **dev-1, dev-3 IDLE.** #698 L7-a still held for Operator.

## 🔄 UPDATES (≤02:15Z 07-01 — 7th fleet-check)
- **ce-ops#381 FIXED → PR #707 approved+merging** (dev-4): automerge-decide path-set now uses `gh pr --json files` (primary) + three-dot fallback → docs PRs classify correctly. Bug fix, NO arming/policy change (verified real diff vs origin/main = 24+/7-, workflow only). **→ L2 is now FUNCTIONAL** (was armed-but-inert): docs-XS/S PRs can now emit AUTO + the actuator merges ce-dev-2-APPROVED ones (still needs my approval per-PR — no unapproved-merge autonomy; that's Surface-B/Operator). **First real actuator fire = observe on the next approved docs-XS/S PR.** #698 L7-a still held.
- ⚠️⚠️ **ROOT CAUSE of the recurring reviewer stale-base false-positives (now 3×: #695/#706/#707): the MAIN CHECKOUT `/home/cedev2/creator-engine` is on the stale `ce-release-0.3.1-rc2` branch** → reviewers' Read/Grep of "main" files get rc2, not origin/main. **HIGH-PRIORITY NEXT-SESSION: get the main checkout onto origin/main** (check rc2 for uncommitted release work first — git status showed M files + release-staging). Until then: ALWAYS authoritatively verify review "diff/scope" findings via `gh pr view --json files` + fresh three-dot diff (this discipline caught all 3). [[ce-reviewer-stale-base-false-carrier-mismatch]]

## 🔄 UPDATES (≤01:00Z 07-01 — 6th fleet-check)
- **#705 conveyor core MERGED.** **Guard-test harvested → PR #706** (reviewer a01120fc): the `${{ }}`-in-run-block CI guard + wired into validate.yml (prevents #703 regression). Was stuck 1h57m on the glacial in-container gate → I harvested + validated on DGX (first-try GREEN) + freed dev-3.
- ⚠️ **SYSTEMIC: contained-seat in-container `validate-pr` is UNUSABLY SLOW** (one xdist worker → 1-2hr, burns seat context). PATTERN: don't wait on it — harvest the seat's committed branch + validate on the DGX. This is a top motivator for the conveyor daemon (validate on fast hardware). Consider a ticket to fix in-container xdist parallelism.
- Bundle-extract footgun: create bundle + `cat` it out in SEPARATE docker-exec calls (mixing text+binary in one stream corrupts the bundle).
- **ALL seat work harvested.** Open: #698 (L7-a held) + #706 (guard-test, in review). dev-1/3/4 idle.

## 🔄 UPDATES (≤23:45Z)
- **Conveyor slice-1 HARVESTED → PR #705** (reviewer a3c282c3 running): pure harvest-prep core `conveyor.py` (handles the 5 footguns: rm build, branch==carrier-stem, rebase/verify-base, regen carrier+work-class, validate seam) + design doc `.ce/design/conveyor-harvest-push.md` (SAFE-to-automate vs Operator-gated-arming split). NO push/PR/docker/arming wired (designed only). validate-pr GREEN. → approve when review clears.
- dev-4 idle (delivered L7+conveyor ~2h tonight; rest, re-feed next cycle). dev-3 on guard-test (ce-ci-runblock-injection-guard) — check next. dev-1 idle.
- **#704 (L2 spot-check doc) MERGED.** Open PRs: #698 (held) + #705 (conveyor, in review).

## 🔄 UPDATES (≤23:05Z — 5th fleet-check + L2 SPOT-CHECK DONE)
- **L2 FIRST-LIVE SPOT-CHECK COMPLETE** (PR #704, a genuine docs change). Result: ✅ live-data decisioning WORKS + fail-closed (read real author/review/checks/class; correctly GESTURE'd pre-approval). ⚠️ **BUT found a fail-SAFE functionality bug → filed ce-ops#381**: automerge-decide uses a **two-dot** `git diff ${PR_BASE_SHA}..${head}` with the STALE `pull_request.base.sha` → a docs PR branched off a moving main picks up inherited `.github/**` (merged L7 workflows) → misclassified `deploy` → GESTURE. **L2 is ARMED-BUT-INERT: it never wrongly auto-merges (safe), but docs auto-merge never actually FIRES.** Fix = three-dot `...` diff or `gh pr view --json files`. (Minor: work-class read as default S not the declared tiny — body-regex extraction may fail; noted in #381.) **→ Correct the "L2 live" status to "L2 armed, functionally blocked on ce-ops#381."**
- #704 approved → merges via wall daemon (genuine openssh-prereq doc note).
- **3 seats fed this cycle:** dev-4=conveyor-harvest-core (design+slice1), dev-3=ci-runblock-injection-guard test, both working. dev-1 idle. #703 (L7 injection-cleanup) MERGED.

## 🔄 UPDATES (≤21:55Z — 4th fleet-check)
- **All L7 dormant slices MERGED** (#699/#700/#701; main=82b7b8cf). Only **#698 L7-a held for Operator GO**. Board clean. Daemon healthy (log-freshness check confirms — no false alarm).
- **N1d re-scoped HARVESTED**: dev-3's clean Python-only version (new `install_prereqs.py` + verify-path actionable errors, NO frozen-artifact edits). 180 scoped unit tests PASS. Fixed 2 harvest nits (dup work-class line; rc2-gate wants old name → `S`→`story`). Worktree `.ce/wt-n1d-harvest` (branch ce-n1d-rescoped). Full validate-pr running (b5qj2nbuz) → push+PR when green.
  - ⚠️ **rc2-install gate quirk**: local validate-pr uses the rc2 editable package → its work-class gate only accepts tiny/story/feature/epic (not XS/S/M/L). Declare OLD names in carriers until the main checkout leaves rc2, OR trust CI (origin/main gate has the compat shim #379).
- **dev-4 re-fed**: L7 injection-cleanup follow-on (move `${{ }}` out of run: blocks → env indirection; release-finalize.yml + release.yml). Brief /var/tmp/dispatch-L7cleanup.md sha 66ce9bb. Branch ce-l7-injection-cleanup. Working.
- **dev-1 + dev-3 IDLE** (available; not re-fed — major arc done, remaining lanes marginal + context-deep). Next-cycle candidates: conveyor harvest→push daemon (genuine value — mechanizes the manual harvest I keep doing), ce-ops#378, L2 spot-check (controller).

## 🔄 UPDATES (≤20:55Z — 3rd fleet-check)
- **#697 MERGED** (per-arch base digests live, main=0edfac378). **L7: #700 MERGED**; #699/#701 APPROVED+merging; **#698 (L7-a) still HELD for Operator GO**.
- ⚠️ **DAEMON false-alarm**: my pgrep liveness check false-negatived → I spawned a DUPLICATE daemon (killed it; single healthy PID 3292408, index 60, clean). **LESSON: check `stat ~/ce-wall-daemon.log` mtime BEFORE restarting** (fresh mtime = alive); pgrep alone is unreliable. [[ce-wall-daemon-token-expiry-restart]]
- 🚨 **N1d QUALITY CATCH (caught at harvest, NOT pushed)**: dev-3's N1d edited docs/install.sh → mirrored into docs/downloads/0.3.1/ → broke the **signed live install** (SHA256SUMS da44ce6b→6768ec7c while signed spec still pins da44ce6b → artifact_hash_mismatch). Deleted the flawed branch (never reached origin). **Re-scoped dev-3 to Python-verify-path-ONLY** (install_spec_signature_guard/update/v3_cli + tests; NO install.sh/downloads). New rule: [[ce-install-sh-coupled-to-signed-release]] — install.sh edits = a RELEASE op (bump+sign), belongs in L7 pipeline. dev-3 now working the re-scope.
- **SEATS:** dev-3 working (re-scoped N1d). **dev-1 + dev-4 IDLE** (shipped #697 / L7) — available, NOT re-fed this cycle (quality focus during the N1d incident); re-feed next cycle (candidates: L7 run-block-injection cleanup follow-on, conveyor daemon, ce-ops#378).

## 🔄 UPDATES (≤20:36Z — L7 reviewed + merge-resolved)
- L7 cohesive security review (a8e7f7cf): #698✓ #700✓ #701✓; **#699 had 1 BLOCKING defect (unused issues:write)** → controller harvest-fixup a5df40a5 (removed perm + test, 8 pass).
- **#699/#700/#701 APPROVED → daemon merging** (dormant machinery: finalize/parity/test, fire only on explicit dispatch). **#698 L7-a HELD — NOT approved — awaiting Operator GO** (auto-tag arming). ⏸️ AWAITING-OPERATOR: the single L7-a auto-tag GO/NO-GO.
- Non-blocking follow-on: run-block `tag_name` interpolation in release-finalize.yml + pre-existing release.yml — regex-safe, cleanup later.
- L7-b finalize approval step needs **CE_RELEASE_REVIEWER_TOKEN** (absent → step fails visibly; release auto-approve inert until provisioned).

## 🔄 UPDATES (≤20:25Z — L7 harvest + #697)
- **#697 (ce-ops#377 per-arch base digests, dev-1)** — reviewed APPROVE (6/6), CI green, **approved as ce-dev-2 → merging**. dev-1 idle.
- **dev-4 L7 DELIVERED** (1h27m, 4 slices all validate-pr GREEN) → harvested via git-bundle out of container → pushed → **PRs #698 (L7-a auto-tag), #699 (L7-b finalize), #700 (L7-e parity), #701 (L7-f integration)**. Cohesive security reviewer running (**a8e7f7cf86a54027a**). dev-4 idle.
  - **MERGE PLAN (authority-gated):** L7-b/e/f (#699/#700/#701) fire only on explicit dispatch/workflow_run (dormant until invoked) → after review APPROVE+merge. **L7-a (#698) AUTO-TAGS on every main push = live-behavior ARMING (beyond G5 build) → HOLD for Operator GO; do NOT self-approve** (ce-dev-2 approve = auto-merge). Surface as the one L7 GO/NO-GO.
  - L7 review worktrees: `.ce/wt-ce-l7{a,b,e,f}-*-review`. May need rebase before merge as main advances (#697 etc.).
- ⚠️ rc2-divergence landmine still open (divergent 0.3.1 wheel) — L7-e parity guards exactly this once armed.

## 🔄 UPDATES (≤20:05Z — 2nd fleet-check)
- All 3 seats WORKING: **dev-4** L7 (4 sub-agents on validate-pr; L7-a/e branches exist), **dev-3** N1d (recovered from venv trap, test suite running, commit 31c9d8a), **dev-1** re-fed **ce-ops#377 per-arch base digests** (brief ~/dispatch-ce377.md sha 621640ff, branch ce-377-per-arch-base-digests, self-pushes → I review/approve). Daemon ALIVE. Board empty.
- ⏳ **L2 first-live spot-check STILL OWED (Operator-asked)** — no organic XS/S docs PR appeared. NEXT-CYCLE top controller action: make ONE genuinely-useful tiny XS docs PR, approve as ce-dev-2, confirm automerge-decide emits AUTO (live data) + actuator merges; audit the decision record. (NOT a seat task.)

## 🔄 UPDATES (≤19:25Z)
- **#696 N1.5 MERGED** (main=33aa6b522) → live docs rendering to HTML. Task done.
- **N1c = ALL PASS** (dev-1 functional e2e vs LIVE): spec fetch ✓, signature ✓, **one-liner install ✓ (14 dl / 13 verified / 1 installed / 0 failed)**, ce verify-install ✓. **The NVIDIA demo install path is PROVEN green.** dev-1 now IDLE (re-feed next cycle).
- **dev-4 L7**: producing slice branches in container /var/tmp worktrees — **ce-l7e-parity @ 56e0e89d** done (release-parity.yml+test+carrier); L7-a/L7-e hit an examples/carrier-conformance gate it's self-fixing. Still working (foreman, fanning out). Harvest all L7 branches once it declares the set done (git-bundle out of container).
- **dev-3 N1d**: hit the contained-seat worktree-venv trap (fresh /var/tmp venv lacks pip+xdist). **Sent correction**: use main venv `/workspace/creator-engine/.venv/bin/python` + `PYTHONPATH=$PWD/validators` for validate-pr; don't pip-install. ⚠️ GENERAL FIX for contained-seat dispatch briefs: tell seats to run validate-pr via the populated /workspace venv + PYTHONPATH=worktree/validators, NOT a worktree-local venv.

## 🚧 IN-FLIGHT — seats WORKING (HARVEST next cycle; DON'T re-dispatch)
- **dev-4** (ce-dgx-codex, foreman) → **L7 auto-release build**. Brief in container `/var/tmp/dispatch-L7.md` (sha 245efc3f) + host `.ce/briefs/dispatch-L7-dev4-20260630.md`. Slices→branches: ce-l7a-auto-tag, ce-l7b-finalize(+L7-c approval step), ce-l7e-parity, ce-l7f-test. STOPS before push → harvest each.
- **dev-3** (ce-vps-codex) → **N1d ssh-keygen preflight**. Brief `/var/tmp/dispatch-N1d.md` (sha 9c1e5e59). Branch ce-n1d-sshkeygen-preflight. Edits docs/install.sh + py verify path. STOPS before push → harvest.
- **dev-1** (non-contained, tmux 2.0) → **N1c functional e2e** install verification vs LIVE (REPORT-ONLY, no install.sh edit). On branch ce-L1-install-doc-fix. Returns PASS/FAIL report → read pane next cycle.
- **#696** N1.5 docs-HTML PR (epic, 10 files, MERGEABLE) → reviewer agent **a737d35e053ef3740** running (quality+confidentiality). On APPROVE → approve as ce-dev-2 → merges.

## ⏭️ REMAINING / NEXT CYCLE
- Harvest dev-4 L7 branches (push→PR→review→approve, per slice). dev-3 N1d (push→PR→review). Read dev-1 N1c report (route fix if FAIL).
- **L2 first-live spot-check** (Operator-asked): create 1 tiny XS docs PR, approve, watch automerge-actuate auto-merge it, audit decision record.
- **L3 apply**: sentinel created on ce-ops#67. Blocked on CE_CROSS_REPO_TOKEN.
- **Surface B**: built-not-armed; Operator-gated; dev-1 belt daemons inactive.

## 🔴 NEEDS OPERATOR (parked)
- R4: Nitzan handle+scope (N1e). - **CE_CROSS_REPO_TOKEN** (unblocks L3-apply + close-bot#262). - **CE_RELEASE_REVIEWER_TOKEN** (L7-c). - Surface-B flip GO.

## SEAT DISPATCH MECHANICS (confirmed this session)
herdr runs INSIDE each container at `/run/creator-engine/herdr/herdr.sock` (NOT a host socket). Deliver brief: `cat brief | [ssh dev1] sudo docker exec -i <ctr> bash -lc 'tee /var/tmp/x.md; sha256sum ...'`. Send pointer: base64-encode the msg (avoids ssh/docker quote-hell), decode inside, `herdr agent send w1:p1 "$P"` then `herdr pane send-keys w1:p1 Enter`. Verify landed via "◦ Working" indicator. dev-1=tmux `send-keys -t ce-dev1-orchestrator:2.0 -l` (base64 same trick).
