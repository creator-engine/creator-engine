# NIGHT-ARC MANDATE — CE-DEV-2 — 2026-07-07 night → 07-08 morning (RATIFIED by Operator 2026-07-07 ~22:xxZ — verbatim, + overnight gate authority explicit)

Frame: continuation of the ratified D1-D8 program. C5 cutover DONE and soaking;
tonight = drain the remediation board through the containerized gate, assemble
promotion evidence, advance set-pieces. Supersedes nothing; extends
RESUME_STATE_CE_DEV2_DAYARC_20260707T18xxZ.

## LANES
- **N-A CONVEYOR (primary):** re-review every remediated head as it pushes (dev-1's
  #883/#884/#886/#887; dev-4's #888 round-2; incoming Option A design PR; dev-3's
  follow-ups unit) → independent reviewer per PR, verdicts posted, APPROVE = gate act
  (approval auto-merges via containerized daemon, standing authority). Target: board
  drained to zero by morning. Harvest-fast doctrine in force (runsc RAM volatility).
- **N-B C5 EVIDENCE:** each merge = authored soak cycle. After cycle 3+, append final
  addendum to C5_PARITY_ASSESSMENT_20260707.md. PROMOTION DECLARATION STAYS OPERATOR —
  evidence assembly only, host daemon stays warm.
- **N-C SET-PIECES:** D4 Ring-1 promotion-evidence packet ASSEMBLY (no cell flip).
  D3 shadow canary: second containerized daemon in dry_run=true against the live repo,
  decision-parity log vs the armed daemon (zero authority; pure observer). D6 drill #1:
  ONLY IF codex standby verifies live+authed (tmux ce-controller session) AND the #874
  harness supports a no-gate-authority drill run; otherwise defer to morning with a
  liveness report.
- **N-D INTAKE:** restock seats as they free. Queue (priority order): dev-4 → #888
  remediation (brief staged); then #500 slice (c) launcher-/tmp-staging fix (small,
  self-contained); #499 seat-side preflight verb design; #494 leftover if dev-3's unit
  misses anything. Territory-check before every dispatch; batch ~lines(200/400).
- **N-E 0.3.4 PREP:** assemble-only — candidate list (today's 13+ merges + tonight's),
  changelog collation, staging notes. CUT + SIGN = Operator co-sign ceremony, morning.

## HARD STOPS (unchanged + tonight's additions)
No external comms/sends (Arad, Nitzan). No signing (ce-root-v1 non-delegable). No
dep-unlock arming. No promotion cell flips. No C5 promotion declaration. No
mythos/settings changes. Gate never leaves CE-DEV-2; merge gate stays singleton.
Seat relaunches canonical-only. Option A stays DESIGN-ONLY — the materialization
authority ruling is an explicit Operator question; no implementation dispatch tonight.
Host-resource rules in force on the VPS (serialize full suites, TMPDIR on disk,
bounded -n).

## ⏸️ MORNING OPERATOR QUEUE (surface FIRST)
1. Arad send: tenant-repo refresh apply + your T4 pack + md-sources decision (#885 merged; tool-side DONE).
2. C5 promotion call (assessment + tonight's cycles will be on disk).
3. Option A materialization-authority ruling (design PR's Open Operator Questions).
4. Ring-1 cell flip (packet will be assembled) + dep-unlock arming.
5. 0.3.4 cut + co-sign.
6. Nitzan D6 answers (most overdue) + #474 tenant half.
7. ce-ops#184 root tmpfs fix (needs root; tonight's OOM root cause).

## WATCH/RESILIENCE
Board watcher (head-aware), seat watcher, C5 failure watcher armed; monitor from prior
session still live. Checkpoint + /clear when context passes the line; resume state
dual-written each checkpoint. OOM playbook: memory ce-runsc-seat-worktree-in-memory-loss.
