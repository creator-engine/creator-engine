# RESUME STATE — CE-DEV-2 · 2026-06-22 (night) · 🌙 DRIVE NIGHT-ARC N1 (full fleet install + program)

**WRITTEN BY/WHERE:** CE-DEV-2 controller as `cedev2` on the **DGX `spark-b824`** (dgx-spark-1 / 100.100.105.50, GB10, aarch64), cwd `/home/cedev2/creator-engine`, Opus 4.8 effort-high. **SUPERSEDES** `RESUME_STATE_CE_DEV2_20260622_FLEET-RETIREMENT-PROGRAM.md`. **READ THIS + MEMORY.md (esp. [[ce-fleet-retirement-clean-install-program]], [[ce-controller-inlines-execution-drift]]) FIRST.** origin/main ≈ `e76508b7`.

## ✅✅ MILESTONE — FLEET CONVERSION (M1) COMPLETE (2026-06-22 ~20:05)
**The night's core mandate is DONE.** dev-4 + dev-3 + dev-1 ALL on clean signed `0.2.0` installs (`~/.local/bin/ce`, provenance `sha256s_sha256==6cd2baf3`), governed codex controllers running (gpt-5.5 **high**), `~/.ce-keys` intact, rollback backups retained. Each proved a REAL merged end-to-end acceptance cycle (pickup→claim→work→PR→merge) via the installed `ce`:
- **dev-4 → #355** (fix ce-ops#211 Node-24) MERGED · **dev-3 → #357** (docs ce-ops#157 context-obs) MERGED · **dev-1 → #358** (fix ce-ops#88 pane-record) MERGED. main @ `10dc9226`.
- **W1 ✅  W2 ✅.  W3:** dev-4 HOLD lifted; dev-3+dev-1 HOLD-lift in flight (worker adfb6b298 → resume proactive governed pickup) → on confirm = **W3 done, M1 fleet-wide**.
- **W4:** #207 design banked; **unit-W1 #356 MERGED** (VisibilityBackend seam); units W2-W5 **HELD** for Operator M2-arch ruling (§5.A/§5.G controller operator_inspectable-headless; §5.B auto-degrade).
- **W5:** #197 `ce onboard` design banked (`DESIGN_197_CE_ONBOARD.md`, 6 units); **BUILD HELD** for Operator (HARD: install-mode default=print / NO autonomous install-exec; profile-edit default; _versions.py classification).
- **W6 remaining:** #209 merge-queue flake; land parked #351/#337/#344 (+ dev-3's #349); file new gaps. Pre-draw morning arc.

### 🚩 GAPS SURFACED TONIGHT (for Operator AM) — all from real dogfooding:
1. **ce-ops#212 FILED** — launcher governed-relaunch fragility (bare-`codex` PATH→exit127; stale `alive` record; double-spawn).
2. **CI does NOT enforce `.ce/` carrier PRESENCE** — "Validate governance artifacts" passed #358 with ZERO manifest/changelog; only dev-3's human review caught it. File CI-gate fix (require presence).
3. **Recurring: fleet PR-open flow omits the `- **Declared work class:**` line** (3x tonight) — PR template / lane PR-open helper should inject it.
4. **DECISIONS HELD:** 2× #207 M2-arch (controller-headless, auto-degrade) + 3× #197 `ce onboard` (install-mode/profile/_versions). Both design docs in `.ce/state/research/`.
**GOVERNANCE HELD UP under autonomy:** reviewer dev-3 caught a real artifact gap; a nudge-worker refused my wrong-premised approve directive (audit doctrine). Independent review + merge-gate intact throughout.
**W3 DONE — fleet in steady-state autonomous governed pickup** (dev-3 on #344, dev-1 polling belt+review-claim #349, dev-4 lifted). Controller holds merge gate. Gap tickets filed: **#212** (launcher), **#213** (CI carrier-presence enforcement), **#214** (PR-open work-class injection).
**Active machinery:** hourly heartbeat cron `dc4cb55f` + controller merge-gate. Fleet works autonomously; I gate merges + monitor.

## 🌅 MORNING ARC N2 — PRE-DRAW (for Operator ratification)
0. **⏸️ AWAITING-OPERATOR — PR #349** (`fix(ce-ops#141)` live-site Docs-nav, ce-dev-3, ce-dev-1-APPROVED + CI green) is **HELD by controller, NOT merged**: it edits `docs/index.html` but (a) lacks the required `site-archive/` snapshot + ledger (website-versioning policy), and (b) per web-design rules a live-site change needs Opus review + a **visual checkpoint** (codex-seat review doesn't satisfy). These aren't machine-enforced. Decide: add snapshot + visual-check then merge, OR waive. (⏸️ comment posted on the PR.)
### 🟢 VISIBILITY RULINGS (Operator 2026-06-23) — D1+D2 RESOLVED → see [[ce-visibility-channel-emission-model]]
- **Contract = read-model emission** (from authoritative governance/lifecycle events, NOT screen-scraping — herdr guardrail). tmux/cockpit/TUI/WebUI/comms-channels = interchangeable renderers of the L1→L2 spine.
- **Headless OK incl. CONTROLLERS** (= read-model/channel-visible, not dark). C4 refines to refuse *dark* headless (`--print`, no surface) only.
- **Default INVERTS:** headless + read-model is the baseline for every lane/controller; per-lane tmux pane / live-attach = OPT-IN extra (the 5% Dev-mode peek). C1 gate asks "emitting to read-model?" not "has tmux pane?".
- **No auto-degrade knob** — moot (CE always ships TUI/WebUI). **Transition rule:** keep tmux as default renderer ONLY until the cockpit renders live lane/controller state from the read-model (coverage worker a6b3ec14 verifying), then flip to headless-baseline. → #207 build target = headless backend EMITS to read-model; + channel-emission/contact-on-need layer is first-class (strangeLoop/CEO surface).

1. **Ratify the remaining HELD decisions** (both design docs in `.ce/state/research/`): #207 §5.A/§5.G (may a governed *controller* declare `operator_inspectable` headless?) + §5.B (auto-degrade tmux→headless?); #197 install-mode (recommend default=`print`, NO autonomous install-exec) + profile-edit default + `_versions.py` classification of the 3 new onboard modules.
2. **On ratification — BUILD:** W4 #207 units W2-W5 (headless VisibilityBackend → relax C1 → schema/validator) → **#208 CE container image** (consume signed wheel, gVisor/OpenShell, brain-init first-run) → prove ≥1 seat containerized/tmux-free (the M2 gateway). W5 #197 `ce onboard` 6 units (PR-1 verify-install → … → PR-6 launcher refuse-before-spawn = the #212 fix).
3. **W6 finish:** #209 merge-queue flake (test_seat_sentinel 10s timeout); land parked #351 (ce157 drain — FIRST resolve already-merged-via-#300 vs divergent), #337 (#151 reconciler), #344 (dev-3), #349 (dev-3 docs-nav); address #212/#213/#214.
4. **Confirm the `ce update loop`** — push a patch release, have a seat pull+adopt it (the one W3 sub-item not yet exercised).
5. Fleet continues autonomous governed operation throughout; controller stocks belt + holds merge gate.

## 🕒 OVERNIGHT FLEET OUTPUT (heartbeats, post-M1) — for Operator review pass
- Fleet (unleashed on proactive pickup) is producing a batch of **governance-DESIGN docs** off the process/role belt (#83 issue-intake contract, #84 ledger-identity/protocols, #89 controller-seat-exclusivity, #104 review-gate venues, #9 role-boundary failsafe, #141 docs-nav, #162 runbook). All docs-only, cross-reviewed (dev-3↔dev-1), manifest+changelog compliant. **MERGED:** #344(#162), #359(#83), #360(#84), #361(#104), #363(#9). **NEEDS-REBASE:** #362(#89, APPROVED but went **DIRTY** — conflicts with merged #360 on ACTIVE_WORK_LEDGER+PANE_REGISTRY protocols; dev-3 to rebase+resolve, then re-merge — deferred to dev-3 autonomy/morning, non-blocking; NOTE: rebase may dismiss the stale approval → re-review). **⏸️ HELD:** #349(#141, AWAITING-OPERATOR live-site).
- 🚩 **Suggest a morning review pass** on this design-doc batch — some may PROPOSE coordination/role policy (not just describe), which could warrant ratification (docs are low-risk/revisable, so merged under R1, but flagging the substance).
- 🚩 **Parallel-edit coordination smell:** two seats edited the SAME protocol docs (#360 & #362) without the [[ce-check-parallel-tracks]] lock protocol — fleet isn't file-locking. Worth reinforcing.

## 📡 NIGHT-SHIFT LIVE STATUS (updated 2026-06-22 night, after handoff)
- **W1 gate:** dev-4 install + governed relaunch = **PASS** (worker a65d30ee: provenance `sha256s_sha256`==`6cd2baf3…`, ver 0.2.0, brain ledger OK, controller relaunched on installed `ce` at directive-compliant `high` — caught+fixed an xhigh override; pane backed up `~/ce-workspaces/relaunch-backup/`; git state identical across restart, no work lost; rollback artifacts intact). ⚠️ caveat: codex+tmux server exited together on the quit keystroke (abrupt but no loss).
- **W1 remaining = dev-4 LIVE acceptance PR:** HOLD lifted for dev-4 (worker af8b09d6 delivered the directive); dev-4 acknowledged + in motion, doing ONE bounded real cycle (pickup→claim→allocate fresh lane off origin/main→work→PR) via installed `ce`, leaving ce157-mint-broker untouched, will open PR + STOP for review. **ON RESUME: check for dev-4's new PR (gh pr list, author ce-dev-4) → route to independent reviewer (dev-1) → merge on APPROVED+green (R1) → that confirms acceptance → promote, keep dev-4 as 2nd reviewer.**
- **W2 dev-3 = PASS** (install+relaunch): canonical `ce launch --harness codex --session dev3-onboard`, gpt-5.5 high, provenance `6cd2baf3`, brain OK, gates fire, backup `~/dev3-preretirement-backup/`. dev-3 now reviewer-capable. Live-acceptance PR (real e2e) NOT yet directed — do after dev-4 canary clears. ⚠️ dev-3's real checkout = `~/creator-engine` (HOME), branch `ce141-docs-nav-refresh`.
- **W2 dev-1 IN FLIGHT** (worker a0363af3) — clean install→brain-init→canonical relaunch (`ce launch --session ce-dev1-orchestrator`). 🛑 PRESERVING `~/.ce-keys` (secondary root `ce-dev1-root-v1` + forge App PEM). VPS `/tmp` 99% full → `TMPDIR=~/.ce-tmp`. On report: gate PASS → dev-1 reviewer-capable again.
- **W4 #207 DESIGN DONE** → `.ce/state/research/DESIGN_207_VISIBILITY_BACKEND.md` (5 PR units; VisibilityBackend registry mirroring RunnerBackend; wave [W1]→[W2,W3]→[W4,W5]). 🚩 **ESCALATE to Operator (do NOT decide autonomously — M2 arch beyond #207/#208):** §5.A/§5.G may a governed CONTROLLER declare `operator_inspectable` headless?; §5.B auto-degrade tmux→headless policy. Building only the conservative subset (headless *worker* lanes, controllers stay tmux, explicit-only).
- **#207 build unit-W1 IN FLIGHT** (worker a7144c6e, isolated worktree) — `VisibilityBackend` registry + `TmuxVisibilityBackend` (ZERO behavior change) + re-point C2 seam; commit-no-push → controller reviews → I push + open PR → route review to dev-3. HOLD units W2-W5 (headless behavior) pending the Operator ruling.
### ✅ MILESTONE: FLEET INSTALL+RELAUNCH DONE (M1 core) — all 3 seats on clean 0.2.0 + governed controllers (gpt-5.5 high), keys intact, backups retained.
- **#355 MERGED ✅ 18:56:35 → W1 COMPLETE.** dev-4 acceptance fully confirmed; ce-ops#211 (Node-24) auto-closed; main @ `85c93304`. **dev-4 HOLD LIFTED (R4)** — first seat back to governed pickup on installed ce.
- **3 PRs in auto-merge sweeper `bmua22txh`** (bg: enqueues each on APPROVED+green, exits when all merged):
  - **#357** (ce-dev-3 acceptance: docs ce-ops#157 context-obs design) — reviewer **dev-1** (independent).
  - **#358** (ce-dev-1 acceptance: fix ce-ops#88 terminalize pane record on lapsed release) — reviewer **dev-3** (independent).
  - **#356** (#207-W1 visibility seam, ce-overwatch) — head `ccfb7cf8` (body+rebase fixed), reviewer **dev-4** (its version_boundary finding resolved). Review-routing worker = aa33affa.
- **ON #357/#358 MERGE:** dev-3 + dev-1 acceptance CONFIRMED → **lift their HOLDs (R4)** (tell each seat to resume proactive pickup) = **W2 DONE + W3** (fleet back to governed pickup on installed ce). dev-3 live pane `dev3-onboard:0.0`; dev-1 live pane `ce-dev1-orchestrator` `controller` win.
- **W4 #207-W1 lesson:** new modules MUST be classified in `validators/creator_engine_validator/_versions.py` (unclassified → `shared` → trips version_boundary ratchet). Bake into W2-W5 build briefs.
- 🚩 **CI ENFORCEMENT GAP (morning ticket / W6):** "Validate governance artifacts" passed on #358 despite ZERO `.ce/pr-manifests/` + `.ce/changelog/` carriers — CI does NOT block on entirely-absent manifest/changelog (only dev-3's human review caught it). Standing rules require them ([[ce-pr-path-manifest-carrier-required]],[[ce-changelog-workflow-obligation]]) but CI doesn't enforce presence → non-compliant PRs can merge if a reviewer misses it. File a CI-gate fix (require the carriers' presence).
- **#357/#358 CI red = G5 work-class line missing** (same as my #356) — authors (dev-3/dev-1) directed to add `- **Declared work class:** <class>` + re-trigger CI (worker ae75de09); sweeper merges on green+approve. 🚩 **RECURRING GAP (3x tonight): the fleet PR-open flow doesn't inject the required work-class line** → file a W6/morning fix (PR template or lane PR-open helper should include `- **Declared work class:** <…>`). NOTE: body edit alone may not re-trigger Validate (needs synchronize/empty-commit push).
- **W5 #197 `ce onboard` DESIGN DONE** → `.ce/state/research/DESIGN_197_CE_ONBOARD.md` (6 PR units; new thin ce kernel cmd composing doctor+install.sh+init+brain-init+launch + ce verify-install + profile-PATH writer). BUILD HELD pending Operator on 3 escalations (HARD: install-mode default=print, NO autonomous install-exec; profile-edit default; _versions.py classification). **Both W4+W5 designs now banked — build when Operator awake.**
- **#356 MERGED ✅** (#207-W1 visibility seam). **#357 MERGED ✅** (dev-3 acceptance CONFIRMED; was compliant w/ manifest+changelog). **#358 (dev-1, branch `ce88-pco-release-pane-terminalize`):** CI green + work-class fixed, but correctly BLOCKED by dev-3 — MISSING `.ce/pr-manifests/` + `.ce/changelog/` carriers (only 2 src files). dev-1 directed to add both (4-path manifest) + push (worker a94ea5e0) → dev-3 re-review→APPROVE → merge (sweeper `bmua22txh` if still alive, else merge manually) → dev-1 acceptance CONFIRMED → **lift dev-3+dev-1 HOLDs (R4) = W2 DONE + W3**. (Reviewer dev-3 caught the gap; my approve-nudge was wrong-premised and the worker correctly HELD it — audit doctrine working.)
- **REMAINING TONIGHT after #357/#358 merge:** lift dev-3/dev-1 HOLDs (resume proactive pickup) → W6 hygiene (#209 flake; land #351/#337/#344; file the recurring PR-open work-class-injection gap) as reviewer capacity frees. W4/W5 BUILDS wait for Operator. Pre-draw morning arc + checkpoint.
- **TICKET ce-ops#212 filed:** launcher relaunch fragility (bare-`codex` PATH → exit-127; stale `alive` record; double-spawn). Permanent fix feeds W5/`ce onboard` (#197). dev-1 immediate unblock = added missing `~/.local/bin` `.bashrc` line (parity w/ dev-3).
- **REVIEWER MAP:** dev-3 ✅ dev-4 ✅ dev-1 ✅ all up + reviewer-capable. dev-3 live pane = `dev3-onboard:0 controller` %66; dev-1 live = `ce-dev1-orchestrator` `controller` win (pid ~2461484). Reap dead panes (dev-3 win1 `codex-ctrl`/%2) later.
- **NEXT BOUNDARY:** once #355 + #356 + dev-3/dev-1 acceptance PRs merge → W1/W2/W3 complete → checkpoint + consider /clear → then W5 (`ce onboard` from #197/#212 catalogue) + W6 (#209 flake, land #351/#337/#344). W4 #207 units W2-W5 HELD pending Operator M2-arch ruling (§5.A/§5.G, §5.B).
- **Monitoring:** hourly heartbeat cron `dc4cb55f` (:17) audits arc + acts. In-session tasks W1–W6 track progress.
- **Operator FINAL instructions (truly signed out):** (1) arm watchers + 60-min cron once fleet driving ✅ done; (2) I am ALSO a controller — pick up arc work-units and drive them via my own workers (W4 started; continue W5/W6 as capacity allows).

## 🟢 THE GO (Operator, 2026-06-22, before /clear)
**Clear GO to (1) clean-install the ENTIRE fleet (dev-4 → dev-3 → dev-1) onto genuine `ce` installs, then (2) drive Night-Arc N1 autonomously.** Both VPS confirmations answered YES (dev-3's missing `~/.ce-keys` is expected/non-blocker; tmux-restart relaunch OK for the non-contained controllers). Mandate = **ce-ops#210** (ratified, approved as-is). Drive per #210; halt only at escalation lines. **Process: delegate execution to WORKERS** (recon/install/transition mechanics) — I coordinate + hold the merge gate + acceptance gate (correction [[ce-controller-inlines-execution-drift]], Operator flagged me inlining today).

## 🚨 BREAK-GLASS VPS ROOT PANE (Operator left this, 2026-06-22 — use ONLY IF ABSOLUTELY NECESSARY)
- **`tmux capture-pane`/`send-keys -t ce-controller:0.1`** = a live SSH session at **`root@ce-pilot-1`** (root on the Hetzner VPS hosting dev-1 + dev-3). Operator left it attached so I CAN reinstall dev-1/dev-3 while they sleep. **DISCIPLINE: only if absolutely necessary** — prefer `ssh dev1`/`ssh dev3` (and `ssh ce-dev-1` passwordless-sudo) for normal worker-driven install mechanics; reserve this root pane for genuine break-glass (e.g. a user-level path can't complete the install). Never leave the VPS in a worse state; record any use here.

## PEER-SEAT → HOST → REACH
- THIS host = DGX. **dev-4 = `ssh cedev4@localhost -i ~/.ssh/id_ed25519`** (`bash -lc`), codex controller tmux `dev4stage1` %0, works `~/ce-workspaces/creator-engine`. **dev-1 = `ssh dev1`** (ce-dev-1@VPS, tmux `ce-dev1-orchestrator`), **dev-3 = `ssh dev3`** (ce-dev-3@VPS, tmux `dev3-onboard`). VPS fallback `ssh ce-dev-1` (=ce@VPS, passwordless sudo to dev-1/dev-3). ⚠️ **dev-3 currently DOWN** (Operator ctrl+c'd it) — do NOT bother resuming; relaunch FRESH as part of its W2 clean-install.
- overwatch gh: `set -a; source ~/.ce-keys/overwatch.env; set +a; export GH_TOKEN=$CE_OVERWATCH_PAT`. Merge queue (`gh pr merge <n> --auto`, no `--squash`). ISSUES=creator-engine/ce-ops; CODE/PRs=creator-engine/creator-engine.

## ✅ DONE TODAY (day-shift) — the install is FIXED + LIVE
- Merged: #348(launch-leg LAUNCHED_STATE) · #350(#206 ce brain init) · #352(--signing-key-id flag) · #354(recipe-principal parameterize) · **#353 (re-published signed 0.2.0)**. Plus earlier #335/#343/#345/#346/#347.
- **RELEASE LIVE:** `creator-engine.dev/downloads/0.2.0/` published + verified (published SHA256SUMS == merged, hash `6cd2baf3…`). Signed 0.2.0 wheel **carries `ce brain init`**, signed by **ce-root-v1**, recipe consistent (ce-root-v1 in grep/awk/verify), full chain verifies end-to-end.
- **DNS ANCHOR DEPLOYED + verified** (was NXDOMAIN → prod install was broken for ALL signers): `_ce-root-v1.creator-engine.dev` TXT = `ce-root-v1=SHA256:mkX7cRfHNrx6mtK8Ek30CcRn6fbfIPK/SU/6KKc0AOQ ce-dev1-root-v1=SHA256:tqPyyLJiJSJA3gdujT2tPv7MiJvdAevxHZSQCdPHC+s` (CF zone `a384e371157c93ad3ab9efe0d6ebc3ed`, rec `4cbbc4bea7420d867bc4303dcb4452bc`). Resolves Status:0, anchor_record() parses for both principals.

## ✅ dev-4 CLEAN INSTALL DONE + VALIDATED (worker a1fc985f, 2026-06-22) — W1 install half complete
Genuine published 0.2.0 installed on dev-4: wheelhouse `sha256s_sha256` flipped **e6460c09 → 6cd2baf3 (== LIVE SHA256SUMS)** = clean-install proof. `ce --version` = `0.2.0+4f4bd35e` (the `+sha` is the published BUILD commit, NOT a dev wheel). Signed-spec trust **VERIFIED** (manual + in-installer): `Good "ce-spec-v1" signature for ce-root-v1`, DNS-anchor bind PASS, canonical floor `d34a296e` matches. `ce brain init` done + verified (genesis ledger in `~/ce-workspaces/creator-engine/.ce/state/brain/assertions.yaml`). Controller tmux `dev4stage1` UNTOUCHED. Deliverables on dev-4: `~/ce-install-w1.cast`/`.log` (+ `-confirm`), `~/ce-install-w1-driver.sh`. **REMAINING W1: governed relaunch dev-4 on installed `ce` (tmux restart, codex effort high) → ACCEPTANCE E2E.** ⚠️ rollback caveat: backup shims were `cp -P` (now resolve to NEW venv); old venv dir `venv-0.2.0-e6460c09…` still on disk → rollback = re-point the `venv` symlink.

## 🎯 ACCEPTANCE GATE (per seat, before promote + next seat) — A1 CORRECTED
Seat completes ONE real e2e cycle on the clean install: **pickup → claim → allocate → launch a governed lane → do work → open a PR**, ZERO source-toolchain / hand-bootstrap. **A1 (CORRECTED): provenance = install-state `sha256s_sha256` == LIVE SHA256SUMS (`6cd2baf3…`) + `pip show` Version 0.2.0 — NOT "absence of +sha" (the published release legitimately reports `0.2.0+<buildsha>`).** A2: governs via installed `ce` not `python -m`. A4: keep rollback (old venv dir + `~/cedev4-preretirement-backup/`) until A3 passes.

## 📝 #197 ONBOARDING CATALOGUE (from dev-4 install — feeds W5 `ce onboard`)
Stitched install path (verbatim in dev-4 `~/ce-install-w1.log`): §0 manual trust-verify (curl spec+trust-root+DNS-anchor → grep/sed/awk fingerprint bind → ssh-keygen -Y verify) THEN `curl …/install.sh | bash` (re-verifies internally, hash-checks 12 wheels, builds venv, exposes ce/cev3, runs `cev3 onboard --inventory`) THEN `ce brain init`. **Friction to fix in W5/#197:** (1) install-lock collision when 2 agents install concurrently → confusing refusal + garbled logs (clearer "install in progress" UX); (2) `+buildsha` version suffix is an ambiguous provenance signal — add `ce verify-install`; (3) §0 sed/awk/grep one-liners are brittle to hand-type (quoting-hostile) — the `install.sh` one-liner is the sane path; (4) no single `ce` self-check for "am I the published release". The `ce onboard` one-shot should wrap §0+install+brain-init into one guided, quoting-safe flow.

## 🖥️ VPS READINESS (audited 2026-06-22 — do NOT re-probe; both lower-risk than dev-4)
- **dev-1 + dev-3:** source-only (no `ce`) → clean install applies; **in-flight work CLEAN** (nothing to drain); egress ✅ + DNS anchor resolves from VPS ✅; codex controllers are **plain codex-in-tmux, NOT bwrap-contained** → relaunch = tmux codex-restart (Operator-confirmed). NO passwordless sudo for dev users (NOT needed — install is user-level `~/.local`; `ce` user has sudo if ever needed).
- **Identity:** dev-1 = codex auth + gh(ce-dev-1) + `~/.ce-keys` (ce-dev1-root-v1 secondary root + ce-forge-dev1 App). dev-3 = codex auth + gh(ce-dev-3), **no `~/.ce-keys` (EXPECTED — not a release-signer, forge identity = its gh token; per-dev App wiring is a separate ce-ops#6/#137 task, NOT M1)**.
- **Sequence (preserve ≥1 reviewer-capable seat):** dev-4 (W1) → reinstall **dev-3** (dev-1 reviews) → reinstall **dev-1** (dev-3 reviews). dev-4 reviews available once it's back up green.

## 🛠️ MECHANICS COOKBOOK (so night-me doesn't re-derive)
- **Sign a release (ce-root-v1, passphrase non-interactive):** `SSH_ASKPASS=/tmp/ce_askpass.sh SSH_ASKPASS_REQUIRE=force DISPLAY=:0 setsid -w ssh-keygen -Y sign -f ~/.ce-keys/ce-root-v1 -I ce-root-v1 -n ce-spec-v1 - < <canonical> > <sig>` (askpass cats `~/.ce-keys/ce-root-v1.pass`; recreate `/tmp/ce_askpass.sh` if gone: `printf '#!/bin/bash\ncat ~/.ce-keys/ce-root-v1.pass\n' > /tmp/ce_askpass.sh; chmod 700`). Verify: `ssh-keygen -Y verify -f docs/keys/ce-root-v1 -I ce-root-v1 -n ce-spec-v1 -s <sig> < <canonical>`.
- **Re-publish a release:** `PYTHONPATH=validators .venv/bin/python -m creator_engine_validator release-stage --repo-root <main> --version 0.2.0 --out <dir> --sign-mode placeholder --signing-key-id ce-root-v1 --force` → sign canonical → insert b64 sig into `value:` → commit `docs/` (OVERLAY: preserve `docs/downloads/0.2.0/scanners/`) → merge → Pages auto-redeploys. Work-class for a release PR = **`feature`** (binary+text diff floor). Manifest needs the fenced ```text block (COUNT + SHA256 of sorted-unique-paths+"\n").
- **CF DNS (token `~/.ce-keys/cloudflare.env` has DNS:Edit; zone `a384e371…`):** `curl -X POST -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" .../zones/<zid>/dns_records --data '{"type":"TXT","name":"_ce-root-v1","content":"...","ttl":300}'`.
- **Merge-queue flake:** `test_seat_sentinel` 10s-timeout flakes the `merge_group` check (#209). Monitors must auto-re-enqueue on a `merge_group` failure that isn't in-progress (pattern used all day). Fix #209 in W6.
- **Force-push:** `--force-with-lease=<branch>:<remoteSHA>` (get remoteSHA via `git ls-remote origin <branch>` first — bare `--force-with-lease` says stale).
- **Branch protection:** main = enforce_admins:true + required_reviews:1 → EVERY merge needs an independent dev-seat APPROVED review (no admin override). Route to a non-author seat; I hold merge.

## 📋 N1 WORK-UNITS (ce-ops#210) + GRANTS
W1 land release+dev-4 canary (release DONE; dev-4 install in flight) · W2 M1 rollout dev-3→dev-1 · W3 fleet→governed-on-installed-ce (lift HOLD, resume pickup) · W4 M2 gateway (#207 visibility backend→#208 container image; target=1 seat proven containerized/tmux-free) · W5 #197 `ce onboard` one-shot (from W1 catalogue) · W6 hygiene(#209 flake, #211 Node20, land #351/#337/#344)+next arc.
**Grants:** R1 auto-merge on review+green · R2 re-sign 0.2.0 line (NO version bump) · R3 seat-retirement runbook · R4 lift HOLD per-seat · R5 trust-anchor DNS via stored token · R6 worker fan-out. **ESCALATE:** acceptance-fail/rollback · version bump or new external publish · onboarding-agent autonomous-EXECUTION authority · M2 arch beyond #207/#208 · Arad/chmod735 (postponed 27 Jun) · <1 reviewer seat.

## OPEN/PARKED PRs · TICKETS
- PRs: **#351** (ce157 drain, 8 commits — needs manifest+review to merge, post-canary) · **#337** (#151 reconciler, rebase+CI+dev-4-reapprove) · **#344** (#162 runbook, dev-3's). 
- Tickets filed today: #205(launch-leg harness) #206(brain init✓) #207(visibility backend) #208(container image) #209(flake) #210(arc) #211(Node20). 
- Fleet is HELD (proactive pickup stopped); lift per-seat (R4) as each passes acceptance.

## STRATEGIC (standing)
- Arad test install POSTPONED → **Sat 27 Jun 2026**; re-probe chmod735 Team upgrade scheduled (durable cron may not survive — re-arm). Out of scope tonight.
- **Codex effort DEFAULT = high** (not xhigh) per 2026-06-22 benchmarks; every codex relaunch tonight = `--harness codex` effort high. Claude controllers also high.
- Journey model: `CE_USER_JOURNEY_MODEL.md`. dev-4 runbook: `RUNBOOK_DEV4_RETIREMENT_M1.md`.

## NEXT-SESSION FIRST ACTIONS (ordered)
1. Check worker `a1fc985f` / re-verify dev-4 install (`ce --version` = published; brain ledger present; trust chain verified). Capture the #197 command catalogue from its report.
2. Governed-relaunch dev-4 on installed `ce` (tmux restart, codex effort high) → run the ACCEPTANCE E2E (real pickup→claim→allocate→launch→work→PR). If pass → promote + lift dev-4 HOLD (R4).
3. W2: dispatch a WORKER to clean-install dev-3 (then dev-1), same gate; dev-1 reviews dev-3's PRs, dev-3 reviews dev-1's. Relaunch fresh (dev-3 is down).
4. W4 #207 visibility backend → #208 container image (1 seat proven). W5 `ce onboard` from the catalogue. W6 #209 flake + #211 Node20 + land #351/#337/#344.
5. Checkpoint + resume-write at each work-unit boundary; dual-write to CE-DEV-1.
